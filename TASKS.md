# Canvora 开发任务清单

> 配套文档：`PRD.md`（需求全文）、`AGENTS.md`（硬性约定）
> 使用方式：**按顺序做，一个任务做完并自测通过后再勾选。** 不要跳着做，很多任务有前置依赖。
> 每完成一期，必须走一遍 PRD 第 12 节对应的验收清单，通过后才能进入下一期。

---

## 记号说明

- `[ ]` 未开始 / `[x]` 已完成
- **依赖**：必须在此之前完成的任务
- **判据**：怎样算这个任务真的做完了（不是"代码写完了"，而是"能验证它工作了"）
- ⚠️ 标记表示这个任务容易做错，务必对照 PRD 的对应章节

---

# 阶段 0：工程骨架

> 目标：把项目跑起来。这一阶段结束时应该能 `npm run dev` 看到空白页面。

### 0.1 初始化仓库

- [ ] **0.1.1** 在 `C:\Users\qiu\Desktop\Canvora` 下建立工程结构：
  ```
  Canvora/
  ├── PRD.md  TASKS.md  AGENTS.md  README.md  DEPENDENCIES.md
  ├── .gitignore
  ├── package.json                    # 根 workspace
  ├── frontend/                       # Vite + React 19 + TS
  ├── backend/                        # Node 22 + Fastify + TS
  ├── shared/                         # 前后端共用的类型定义
  └── scripts/                        # 启动、下载工具等脚本
  ```
  - **依赖**：无
  - **判据**：目录存在；`git status` 能看到这些文件

- [ ] **0.1.2** 写 `.gitignore`，**必须包含**：`node_modules/`、`dist/`、`secrets.json`、`*.log`、工作区目录、`frontend/.vite/`
  - **判据**：`git status --short` 里看不到 `node_modules` 和 `secrets.json`

- [ ] **0.1.3** 建立 `DEPENDENCIES.md`，按 PRD 6.4 节要求登记依赖（包名 | 版本 | 许可证全称 | 用途）
  - **判据**：文件中已登记第 0 阶段用到的全部依赖

### 0.2 前端骨架

- [ ] **0.2.1** 用 Vite 创建 React 19 + TypeScript 工程到 `frontend/`
  - 安装 Tailwind CSS 4、shadcn/ui、zustand、react-router
  - ⚠️ **不要安装**：tldraw、@xyflow/react、任何画布库（画布自研）；Konva 只在阶段 5 装
  - **判据**：`cd frontend && npm run dev` 能打开页面

- [ ] **0.2.2** 建立基础布局：左侧工具栏、中间画布区（暂时空白）、右侧面板、底部时间轴占位（暂时折叠）
  - **判据**：能看到四个区域，尺寸正常，窗口缩放时布局不破

- [ ] **0.2.3** 建立主题（浅色/深色）与中文界面基础
  - **判据**：切换主题后所有区域颜色正确变化

### 0.3 后端骨架

- [ ] **0.3.1** 创建 `backend/`，Node 22 + Fastify + TypeScript
  - **判据**：`npm run dev` 后访问 `http://127.0.0.1:<port>/api/health` 返回 `{"ok":true}`

- [ ] **0.3.2** 只监听 `127.0.0.1`，不要监听 `0.0.0.0`
  - **判据**：用局域网另一台设备访问该端口访问不到

- [ ] **0.3.3** 建立 SQLite 层（建议 `better-sqlite3`），写建表迁移脚本
  - 表：`projects`、`nodes`、`edges`、`assets`、`tasks`、`timelines`、`tracks`、`clips`、`settings`、`providers`
  - 字段严格对照 PRD 第 8 节
  - **判据**：删掉 db 文件重启能自动重建；表结构用工具能查到

- [ ] **0.3.4** 建立日志模块，写 `logs/canvora-YYYY-MM-DD.log`，保留 7 天
  - ⚠️ **必须**：写日志前经过密钥脱敏函数
  - **判据**：日志文件出现；把假密钥传给日志函数，文件里搜不到该密钥

- [ ] **0.3.5** 建立统一的错误响应格式（含中文 `message` + 技术 `detail` 两个字段）
  - **判据**：故意触发一个错误，前端能拿到中文说明

### 0.4 前后端联调与启动脚本

- [ ] **0.4.1** 建立 `frontend/src/api/` 作为**唯一**的前后端边界
  - ⚠️ 前端任何地方都不允许出现裸 `fetch`。加一条 ESLint 规则禁止 `fetch`，只允许从 `src/api/` 导出
  - **判据**：全局搜索 `fetch(` 只出现在 `src/api/` 下

- [ ] **0.4.2** `shared/types/` 放 PRD 第 8 节的全部接口定义，前后端各自引用
  - **判据**：改一个字段名，前后端同时报类型错误（说明确实共享了类型）

- [ ] **0.4.3** 写 `启动.bat`：检查依赖是否装好 → 启动后端 → 启动前端 → 自动打开浏览器
  - **判据**：双击后 10 秒内浏览器出现界面

### 0.5 工具获取脚本

- [ ] **0.5.1** 写 `scripts/fetch-tools.mjs`：下载 `realesrgan-ncnn-vulkan`（Real-ESRGAN v0.2.5.0 的 Windows zip）与 `rife-ncnn-vulkan` 到工作区 `bin/`，解压，校验可执行
  - **判据**：运行后 `bin/` 下存在两个 exe，且能执行 `--help`

- [ ] **0.5.2** 写工作区检测模块：列出所有磁盘及剩余空间（**不要用 wmic，已被 Windows 11 移除**，用 Node 的 `fs.statfs` 或 PowerShell `Get-CimInstance`）
  - **判据**：能正确列出 C/D/F/G 四个盘及剩余 GB

---

# 阶段 1（P0 第一部分）：工作区、素材、项目

> 目标：能导入素材、能建项目、能浏览素材库。此阶段结束时已有可用的"素材管理"。

### 1.1 工作区初始化（PRD A1）

- [ ] **1.1.1** 首次启动引导页：列出磁盘剩余空间，**默认选中剩余最大的盘**，C 盘低于 30 GB 标红"不推荐"
  - **判据**：全新环境下引导页出现，F 盘（288 GB）被默认选中

- [ ] **1.1.2** 按 PRD 6.7 节创建完整目录结构（`bin/`、`models/`、`luts/`、`cache/thumbs/`、`projects/`）
  - **判据**：选择 `F:/Canvora` 后目录结构完整出现

- [ ] **1.1.3** 环境检测：ffmpeg / ffprobe 是否存在；`bin/` 下本地 AI 二进制是否存在，缺失时提供一键下载
  - **判据**：临时把 PATH 里的 ffmpeg 藏掉，引导页给出明确提示

- [ ] **1.1.4** 磁盘剩余低于 20 GB 时黄色警告，每次启动都提醒
  - **判据**：把工作区设到一个剩余很小的目录，警告出现

### 1.2 素材导入（PRD A2）

- [ ] **1.2.1** 拖拽文件到画布导入；拖拽文件夹导入；顶部按钮文件选择器（多选）
  - **判据**：拖 5 个混合格式文件，全部导入成功

- [ ] **1.2.2** 用 ffprobe 读取元信息：宽高、时长、帧率、编码、是否有音轨
  - **判据**：数据库里素材的宽高、时长与实际一致

- [ ] **1.2.3** 素材复制到 `assets/<assetId>.<ext>`，保留原始文件名到数据库
  - ⚠️ 不要只存路径引用
  - **判据**：把原文件移走，Canvora 里素材仍可用

- [ ] **1.2.4** 大文件复制显示进度条，可取消
  - **判据**：导入一个 1 GB 文件，进度条正常，取消后不留半个文件

- [ ] **1.2.5** 导入前检查磁盘空间，不足则整体拒绝并说明还差多少 GB
  - **判据**：构造空间不足场景，导入被拒绝且有具体数字

- [ ] **1.2.6** 按内容哈希检测重复导入，提示用户
  - **判据**：同一文件导入两次，第二次给出提示

- [ ] **1.2.7** 不支持格式逐个提示，其他文件继续导入
  - **判据**：混入一个 `.xyz` 文件，其他正常导入

- [ ] **1.2.8** 导入完成后素材出现在画布上，多个文件以网格排列不重叠
  - **判据**：拖入 6 个文件，画布上整齐排列无重叠

### 1.3 素材归一化（PRD 6.6、10.3）

- [ ] **1.3.1** 检测是否需要归一化（有旋转矩阵 / 非 yuv420p / 非 BT.709 / 编码非 H.264）
  - **判据**：用手机竖屏视频测试，能被识别为需要归一化

- [ ] **1.3.2** 后台排队执行归一化转码，不阻塞导入
  - **判据**：导入后立即可用，归一化在后台完成

- [ ] **1.3.3** 归一化版本用于编辑与预览，原片保留
  - **判据**：`assets/` 下有两个文件

### 1.4 proxy 与缩略图（PRD 10.4）

- [ ] **1.4.1** 视频导入后自动生成 proxy（`scale=-2:480 -crf 30 -g 30`）
  - **判据**：`proxy/` 下出现文件；ffprobe 确认高度 480、关键帧间隔 1 秒

- [ ] **1.4.2** 生成缩略图（取视频第 1 秒处的帧）
  - **判据**：素材库能看到缩略图

- [ ] **1.4.3** `proxyStatus` 状态跟踪，前端据此显示"准备预览中"或播放器
  - **判据**：删除 proxy 文件后状态变为 pending 并重新生成

### 1.5 素材库（PRD A3）

- [ ] **1.5.1** 右侧可折叠面板，"当前项目"与"全部素材"两个页签
  - **判据**：两个页签内容正确区分

- [ ] **1.5.2** 网格视图 + 视频时长角标
- [ ] **1.5.3** 筛选：类型、来源、收藏、标签
- [ ] **1.5.4** 排序：时间 / 大小 / 时长
- [ ] **1.5.5** 拖拽到画布创建节点；双击也在画布创建
- [ ] **1.5.6** 右键菜单：重命名、加标签、收藏、在资源管理器中显示、删除
  - ⚠️ 删除被时间轴引用的素材时必须二次确认
- [ ] **1.5.7** 搜索框：按原始文件名模糊搜索
- [ ] **1.5.8** 素材文件丢失时显示"文件丢失"占位，提供"重新定位"与"移除"
  - **判据**：手动从磁盘删掉一个素材文件，界面出现占位而非崩溃

- **本组判据**：用 20 个素材测试，筛选/排序/搜索/收藏/标签全部生效

### 1.6 多项目管理（PRD A4）

- [ ] **1.6.1** 项目下拉：新建、重命名、复制、删除、切换
- [ ] **1.6.2** 切换时保存并恢复画布视口状态
- [ ] **1.6.3** 删除项目时提示文件数量与体积，需输入项目名二次确认
  - **判据**：建 3 个项目自由切换，内容互不干扰；删除一个项目后其素材文件夹被清理

---

# 阶段 2（P0 第二部分）：无限画布

> 目标：能像用 PureRef 那样浏览素材，能拖动缩放，帧率不掉。

### 2.1 画布视口（PRD B1）

- [ ] **2.1.1** 建立画布容器：**一个祖先 div 统一做 `transform: translate(x,y) scale(z)`**
  - ⚠️ **不要给每个节点单独做位置变换随缩放重算**。这是性能的关键
  - **判据**：缩放时用开发者工具确认没有大量 layout/reflow

- [ ] **2.1.2** 滚轮缩放，**以鼠标指针为锚点**，范围 0.1x ~ 4x
  - ⚠️ 锚点算法容易写错：缩放前后指针下的画布坐标必须一致
  - **判据**：放大缩小来回 10 次，指针下的节点不漂移

- [ ] **2.1.3** 平移：中键拖拽 / 空格+左键拖拽 / 触控板双指
- [ ] **2.1.4** 框选：空白处拖拽出选框
- [ ] **2.1.5** 快捷键：`Ctrl+0` 100%、`Shift+1` 适应全部、`Ctrl+=` / `Ctrl+-`
- [ ] **2.1.6** 右下角显示缩放比例，可点击输入精确值
- [ ] **2.1.7** 小地图：显示节点位置与视口矩形，点击跳转
- [ ] **2.1.8** 背景网格点阵，缩放很小时不画网格
- [ ] **2.1.9** 平移/缩放期间给容器加 `will-change: transform`，停止 200ms 后移除
- [ ] **2.1.10** 视口裁剪：视口外的节点不渲染
  - **判据**：放 200 个节点，平移缩放时视口外的节点确实没渲染（用 React DevTools 确认）

- **本组判据**：100 节点画布上缩放平移流畅，无明显掉帧

### 2.2 节点渲染与交互（PRD B2）

- [ ] **2.2.1** 实现全部节点类型的渲染组件（先做静态展示，控件后接）
  - 类型：prompt / text / image / video / audio / generateImage / generateVideo / llm / upscale / interpolate / extractFrame / group / note
  - ⚠️ 每个节点组件必须 `React.memo`，且**通过 id 精确订阅自己的数据**，不允许订阅整个节点数组
  - **判据**：拖动一个节点时其他节点不重渲染（用 React DevTools 的 highlight updates 确认）

- [ ] **2.2.2** 拖动节点；多选时一起移动
- [ ] **2.2.3** 调整大小（八个控制点）；图片/视频节点按 Shift 保持比例
- [ ] **2.2.4** 选中：单击、Shift 加选、框选、`Ctrl+A` 全选
- [ ] **2.2.5** 复制粘贴：`Ctrl+C` / `Ctrl+V`（偏移 20px）、`Ctrl+D`
- [ ] **2.2.6** 删除：`Delete`
- [ ] **2.2.7** 对齐辅助线：拖动时接近其他节点边缘或中线 8px 内显示辅助线并吸附
- [ ] **2.2.8** 层级：右键"置顶/置底/上移/下移"
- [ ] **2.2.9** 分组：`Ctrl+G` 成组，组可折叠
- [ ] **2.2.10** 便利贴节点：颜色可选，只有文本，不能连线
- [ ] **2.2.11** ⚠️ **视频节点：默认显示缩略图 + 播放按钮，不自动播放**
  - 播放时用自制进度条与时间码；鼠标移开或播放结束回到缩略图
  - **同一时刻只允许一个视频节点处于播放状态**
  - **判据**：放 6 个视频节点，同时点播多个，确认只有最后一个在播

- [ ] **2.2.12** 视频节点右键：抽取首帧 / 尾帧 / 当前帧，在下方创建图片节点
  - **判据**：抽帧结果与视频实际帧一致；耗时小于 1 秒

- **本组判据**：拖动 20 个节点同时移动不卡顿

### 2.3 连线（PRD B3）

- [ ] **2.3.1** 节点悬停显示输出口（右）与输入口（左）
- [ ] **2.3.2** 从输出口拖拽，出现跟随鼠标的贝塞尔曲线
- [ ] **2.3.3** ⚠️ **拖拽过程中实时高亮所有合法输入口，非法输入口变灰**
  - 这是防止用户连错的关键，不要省
  - **判据**：从一个 text 输出口开始拖，只有 text 输入口高亮，image 口是灰的

- [ ] **2.3.4** 连线颜色按 kind 区分：text 灰 / image 蓝 / video 紫 / audio 绿
- [ ] **2.3.5** 连线中段悬停显示删除按钮；选中连线按 `Delete` 删除
- [ ] **2.3.6** 输入口已有连线时新连线替换旧连线，并给出提示
- [ ] **2.3.7** ⚠️ **环检测**：连接前检查是否成环，成环则拒绝并提示"不能连成循环"
  - **判据**：构造 A→B→A，第三次连接被拒绝

- [ ] **2.3.8** 删除节点时，与其相连的所有连线一并删除
- [ ] **2.3.9** 每个节点类型声明自己的输入/输出口及端口类型（对照 PRD 8.3 的端口表）
  - **判据**：生视频节点选"首尾帧"模式时显示两个图片输入口

### 2.4 画布导入导出（PRD B5）

- [ ] **2.4.1** 导出为 `.canvora`（zip，含 `canvas.json`），提供"包含素材"/"仅结构"两个选项
- [ ] **2.4.2** 导入时选择"新建项目"或"追加到当前画布"
- [ ] **2.4.3** 版本号与兼容提示
- [ ] **2.4.4** 损坏文件的容错提示，不崩溃
- **判据**：导出后重新导入，节点/连线/位置完全一致；仅结构导出文件小于 1 MB

---

# 阶段 3（P0 第三部分）：AI 接口与服务商配置

> 目标：能配置服务商、测试连接、拿到可用模型列表。

### 3.1 服务商管理（PRD C1）

- [ ] **3.1.1** 设置页"模型与接口"：服务商列表（名称、协议、模型数、启用状态、测试结果）
- [ ] **3.1.2** 新增服务商表单：协议类型下拉（8 种）、名称、Base URL、API Key（password + 显示切换）
  - **判据**：能保存一个服务商并在列表中看到

- [ ] **3.1.3** 模型列表管理：手动添加模型 ID、显示名、勾选能力标签
  - 能力标签：文生图 / 图生图 / 文生视频 / 图生视频 / 首尾帧 / 图片编辑 / 文本对话
- [ ] **3.1.4** 若协议支持 `/v1/models`，提供"从接口拉取模型列表"按钮
- [ ] **3.1.5** **测试连接**按钮：最小请求验证，成功/失败都给出明确中文说明
  - 错误分类：密钥无效 / 地址无法访问 / 连接超时 / 余额不足 / 权限不足
  - **判据**：故意填错 Key，提示"密钥无效"而不是一串英文报错

- [ ] **3.1.6** 内置预置模板一键添加（阿里百炼、智谱、火山方舟、MiniMax、DeepSeek），预填 Base URL 与默认模型
  - **判据**：选"智谱"预置，只需填 Key 就能用

- [ ] **3.1.7** 预置模板列表里明确标注每家能不能个人开通（PRD 14.3 表）
  - ⚠️ 火山方舟视频、可灵必须标注"个人不可用 / 需审核"

- [ ] **3.1.8** 高级：`自定义脚本` 协议类型，允许用户填自己的请求构造逻辑
  - ⚠️ 必须在界面上明确警告"只填写你自己写的或你信任的脚本"，并做超时与异常隔离

### 3.2 能力与模型映射（PRD C2）

- [ ] **3.2.1** 生成节点的模型下拉只列出具备对应能力的模型
  - **判据**：生视频节点上看不到 `deepseek-chat`

- [ ] **3.2.2** 设置页配置各能力的默认模型
- [ ] **3.2.3** 某能力无可用模型时，下拉框显示"暂无可用模型，点这里去配置"
  - **判据**：清空所有生视频模型，生视频节点给出引导而非空白

### 3.3 密钥管理（PRD H1）

- [ ] **3.3.1** 密钥加密存储到工作区 `secrets.json`；`.gitignore` 已包含
  - **判据**：打开 `secrets.json` 看不到明文 Key

- [ ] **3.3.2** 密钥列表显示尾 4 位、添加时间、测试状态
- [ ] **3.3.3** 界面常驻醒目提示："请只填写设置了消费上限的子密钥，不要使用主账号密钥"
- [ ] **3.3.4** 删除、替换密钥；一键测试全部
- [ ] **3.3.5** 密钥文件损坏时提示重新填写，不阻塞启动
- [ ] **3.3.6** ⚠️ 全项目搜索确认：密钥不出现在任何日志、界面默认状态、提交到 git 的文件中

---

# 阶段 4（P0 第四部分）：生成节点与执行引擎

> 目标：走通 PRD 2.4 的场景一和场景二。这是 P0 的终点。

### 4.1 任务队列与轮询调度器

- [ ] **4.1.1** 定义统一的 `GenerationTask` 抽象（对照 PRD 8.5）
- [ ] **4.1.2** 通用轮询调度器：指数退避、并发上限控制、失败重试、超时判定
  - **默认并发 3**，可在设置里调 1~6
  - **判据**：连续发 5 个云端任务，同时在跑的不超过 3 个

- [ ] **4.1.3** 任务持久化到 SQLite
- [ ] **4.1.4** ⚠️ **应用重启后的恢复逻辑**：
  - 云端 `running` 任务 → 恢复轮询（云端还在跑）
  - 本地 `running` 任务 → 标记失败（子进程已死），提示用户重新执行
  - **判据**：发起一个生视频任务，关掉应用再打开，任务状态正确恢复

- [ ] **4.1.5** ⚠️ **拿到结果立刻下载落盘**，不能只存 URL（URL 会过期）
  - **判据**：生成完成后断网，素材仍可播放

- [ ] **4.1.6** 取消：云端调取消接口；本地杀进程树
  - ⚠️ Windows 上杀进程树用 `taskkill /F /T /PID`，只 kill 直接子进程会留残余
  - **判据**：取消后任务管理器里没有残留的 ffmpeg / realesrgan 进程

### 4.2 服务商适配器

- [ ] **4.2.1** 图像适配器：OpenAI 兼容 `/v1/images/generations`
  - 覆盖阿里兼容模式、智谱 cogview、火山 Seedream、OpenAI
  - 参数映射表放在配置里，不要硬编码模型名
  - **判据**：用智谱 cogview-4 成功生成一张图

- [ ] **4.2.2** 视频适配器：阿里百炼 `services/aigc/video-generation/video-synthesis`
  - 支持文生视频、图生视频、**首尾帧**（`wan2.2-kf2v-flash` 等）
  - **判据**：用两张图作为首尾帧成功生成视频

- [ ] **4.2.3** 视频适配器：智谱 `videos.generations` + 查询
  - **判据**：用 `cogvideox-3` 成功生成视频

- [ ] **4.2.4** 文本适配器：OpenAI 兼容 `/v1/chat/completions`（接 DeepSeek）
  - **判据**：用 DeepSeek 的 Key 成功拿到回复

- [ ] **4.2.5** 每个适配器实现统一接口 `submit()` / `poll()`
  - **判据**：新增一家只需写一个文件，不改调度器

- [ ] **4.2.6** ⚠️ **图片传递策略**：优先 base64；接口只接受 URL 时明确提示用户配置图床
  - **绝不私自把用户图片上传到任何公共图床**
  - **判据**：检查代码，没有任何硬编码的第三方上传地址

### 4.3 执行引擎（PRD B4、10.5）

- [ ] **4.3.1** 拓扑排序（DFS 后序）收集上游子图
- [ ] **4.3.2** ⚠️ **环检测**（三色标记法），发现环时报出环上的节点 id 并高亮
- [ ] **4.3.3** `isDirty` 判定：`lastRunHash`（参数 + 上游结果 ID 哈希）一致且上次成功则跳过
  - **判据**：连点两次运行，第二次跳过已完成的节点（从日志确认）

- [ ] **4.3.4** 按端口 kind 从上游结果中提取输入；缺输入时明确失败提示
- [ ] **4.3.5** 并发限流：本地串行（同时 1 个）；云端按设置；混合时本地等待云端结束
- [ ] **4.3.6** 节点上显示进度环与中文状态文字
- [ ] **4.3.7** 取消功能真正终止后端任务
- [ ] **4.3.8** 失败后提供"重试"与"从失败处重跑"两个选项
- [ ] **4.3.9** 节点可展开查看最近运行记录（时间、耗时、结果）
- [ ] **4.3.10** ⚠️ **执行中关闭页面后端继续执行**；重开页面正确显示进度
  - **判据**：执行中关掉浏览器标签页，等 1 分钟重新打开，进度正确

- [ ] **4.3.11** 错误分类的中文提示全部实现：
  - 上游没配 provider → "提示词节点还没连到任何模型，请先在设置里配置服务商"
  - 余额不足 → "接口返回余额不足，请到服务商后台充值"
  - 内容审核 → "生成被服务商的内容审核拦截"
  - 网络失败 → "网络连接失败，请检查网络后重试"

### 4.4 生成节点

- [ ] **4.4.1** 文本节点（`prompt` / `text` / `note`）：多行文本框、可调大小、超出滚动
- [ ] **4.4.2** "AI 扩写"按钮：调 `llm` 能力把简短提示词扩写详细
  - **判据**：写"一只猫"，点扩写后得到一段详细描述

- [ ] **4.4.3** 生图节点：模型选择、数量 1~4、尺寸、种子、负面提示词、其他参数键值对
- [ ] **4.4.4** ⚠️ 生图完成后**每张结果自动创建一个图片节点**，排在生图节点下方并建立连线
  - **判据**：生成 3 张，画布上出现 3 个图片节点且都有连线指向生图节点

- [ ] **4.4.5** 生图结果自动入库（`source: 'generated'`）
- [ ] **4.4.6** 处理 base64 与 URL 两种返回格式
- [ ] **4.4.7** 部分失败时成功的建节点，失败的标注"3 张中 1 张失败"
- [ ] **4.4.8** 生视频节点：模式选择（文生/图生/首尾帧），模式决定显示哪些输入口
- [ ] **4.4.9** 生视频参数：模型、时长、分辨率、宽高比、帧率、种子（按模型能力动态给出可选项）
- [ ] **4.4.10** ⚠️ 异步进度："排队中 → 生成中（第 N 秒） → 下载中 → 完成"
  - 多数接口不给百分比，进度条用"已等待/预计"估算，**必须标注"预计"**
  - **判据**：进度文字随轮询实时更新

- [ ] **4.4.11** 生成完成后自动排队生成缩略图与 proxy
- [ ] **4.4.12** 抽帧节点：位置选首帧/尾帧/指定秒，可拖滑块预览
- [ ] **4.4.13** 首尾帧两张图尺寸不一致时提示用户，询问是否统一到第一张的尺寸
- [ ] **4.4.14** 任务超过 30 分钟无响应 → 标记超时失败，提供"重新查询一次"按钮

### 4.5 任务中心与日志（PRD H2、H4）

- [ ] **4.5.1** 右下角可折叠任务中心，列出排队/运行/完成/失败
- [ ] **4.5.2** 显示名称、状态、进度、已用时间、预估剩余
- [ ] **4.5.3** 取消单个、取消全部、失败重试
- [ ] **4.5.4** 完成后页面内 toast 通知
- [ ] **4.5.5** 任务可按状态/类型/时间筛选；可定位到对应画布节点
- [ ] **4.5.6** 设置页"查看日志"与"导出诊断包"（含日志+版本+环境+设置，**不含密钥**）
- [ ] **4.5.7** 快捷键速查面板（`?` 或 `F1`），按上下文分组
  - ⚠️ 实现完每个快捷键后回来核对这张表与实际行为一致

---

# 🚩 P0 验收关口

- [ ] **走通 PRD 2.4 场景一**：一句话 → 生成图 → 放大到 4K（放大在 P1，此处先确认生成部分）
- [ ] **走通 PRD 2.4 场景二**：两张图 → 首尾帧生视频 → 画布上能播放
- [ ] 执行 PRD 12.1 / 12.2 / 12.3 / 12.4 的全部验收项
- [ ] 无阻塞性 bug；所有错误都有中文提示
- [ ] `DEPENDENCIES.md` 中每个依赖的许可证都合规

**P0 全部通过后才进入 P1。**

---

# 阶段 5（P1 第一部分）：本地 AI 放大与补帧

> 目标：能把素材放大到 4K、把帧率补上去。这是"省钱"的核心价值。

### 5.1 本地任务队列（PRD E4）

- [ ] **5.1.1** ⚠️ **本地任务严格串行**（同时 1 个），界面上显示"排队中（前面还有 N 个）"
  - **判据**：连点 3 个放大任务，从日志与进程列表确认是依次执行

- [ ] **5.1.2** 子进程封装：用参数数组 spawn，**不拼 shell 字符串**
  - ⚠️ 绝不走 `cmd /c`
- [ ] **5.1.3** 解析 ncnn 的 stdout 进度输出
- [ ] **5.1.4** 取消时杀整个进程树并清理中间文件
- [ ] **5.1.5** 记录每个任务的耗时，用于后续估算剩余时间

### 5.2 图片放大（PRD E1）

- [ ] **5.2.1** 输入图片节点，输出新图片节点（原图保留）
- [ ] **5.2.2** 参数：引擎、倍数（2/3/4）、模型、tile 大小
- [ ] **5.2.3** ⚠️ 界面上按内容类型给出模型建议（对照 PRD 14.1 的模型选择表）
  - 动画 → `realesrgan-x4plus-anime` / Real-CUGAN；实拍 → `realesrgan-x4plus`；不确定 → `realesrgan-x4plus`
  - ⚠️ 必须提示：**Real-CUGAN 与 Anime4K 用在真人实拍上会过度平滑失真**
- [ ] **5.2.4** 显示速度预期提示（如"1024x1024 预计 10~30 秒"）
- [ ] **5.2.5** ⚠️ 输出尺寸超过 16000px 时警告并询问
- [ ] **5.2.6** ⚠️ 显存不足时自动把 tile 减半重试一次，仍失败则提示手动调小
- [ ] **5.2.7** 完成结果自动入库（`source: 'upscaled'`）
- **判据**：1024x1024 图放大 4 倍 → 输出 4096x4096，用图片查看器确认尺寸

### 5.3 视频放大（PRD E2）

- [ ] **5.3.1** ⚠️ **按 PRD 10.6 的分块流程严格实现**：
  1. ffprobe 读时长/帧率/总帧数
  2. 估算空间（总帧数 × 350KB × 2），**超过工作区剩余 50% 则拒绝并给出所需 GB**
  3. 每批 200 帧抽帧，**用 JPEG（`-q:v 2`）而不是 PNG**
  4. 批量调 realesrgan，**每批处理完立即合回视频段并删除中间帧**
  5. 拼接所有段（concat demuxer）
  6. **从原视频复制音轨（`-c:a copy`）**
  7. 清理 `temp/`
  - **判据**：处理完 `temp/` 为空

- [ ] **5.3.2** ⚠️ 进度显示"第 X/Y 帧"，而不是笼统百分比；同时显示已用时间与预估剩余
- [ ] **5.3.3** 界面明确说明这是后台任务，不能实时预览
- [ ] **5.3.4** ⚠️ 视频无音轨时用 `-map 1:a?`（带问号）跳过，不报错
- [ ] **5.3.5** 长视频（>10 分钟）提示可能耗时很久，建议先剪短片段试
- [ ] **5.3.6** 处理期间显示"当前磁盘占用"与"已清理批次数"
- [ ] **5.3.7** 中途失败默认整体清理中间文件
- **判据**：1080p 10 秒视频放大 2 倍，输出可正常播放、有声、时长一致；磁盘峰值不超过预估的 1.2 倍

### 5.4 补帧（PRD E3）

- [ ] **5.4.1** 参数：倍率（2x/4x）、模型（`rife-v4.6` 等）、目标帧率（自动算）
- [ ] **5.4.2** 流程同 5.3，但调 `rife-ncnn-vulkan`
- [ ] **5.4.3** 源视频已 >60fps 时提示没必要补帧
- [ ] **5.4.4** 补帧后文件体积增大时给出提示
- **判据**：30fps 视频补到 60fps，ffprobe 确认输出帧率为 60，画面流畅

---

# 阶段 6（P1 第二部分）：时间轴与剪辑

> 目标：走通 PRD 2.4 的场景三。**这是本项目最核心、最容易做错的部分。**

### 6.1 时间轴基础（PRD F1）

- [ ] **6.1.1** 安装 Konva（**这是本 PRD 中 Konva 的唯一推荐用途**）
- [ ] **6.1.2** 底部时间轴面板，可折叠、可调整高度
- [ ] **6.1.3** ⚠️ **时间轴状态放独立 store，不要和画布状态混在一起**
  - **判据**：画布上拖节点不会导致时间轴重渲染，反之亦然

- [ ] **6.1.4** 从画布拖视频/图片节点到时间轴创建片段；也从素材库拖入
- [ ] **6.1.5** 标尺显示时间刻度（`mm:ss.f`）
- [ ] **6.1.6** 轨道：默认 1 条视频轨 + 1 条贴图轨，可新增
- [ ] **6.1.7** 播放头：可拖拽，拖动时预览画面跟随
  - 标注"预览为关键帧精度，导出为帧精确"
- [ ] **6.1.8** 走带控制：播放/暂停、上一帧/下一帧、到开头/结尾、循环播放
- [ ] **6.1.9** 时间码显示：当前时间与总时长
- [ ] **6.1.10** `Ctrl+滚轮` 缩放时间轴比例；滚轮水平滚动
- [ ] **6.1.11** 片段显示：缩略图条 + 文件名 + 时长；变速片段显示倍速角标
- **判据**：拖 3 个视频形成连续序列，能顺次播放完

### 6.2 分割与删除（PRD F2）

- [ ] **6.2.1** 播放头位置按 `S` 或点"分割"，把片段一分为二
  - ⚠️ 视频轨上同一时间位置的片段要一起切
- [ ] **6.2.2** 选中片段按 `Delete` 删除，**后面片段自动前移补齐空隙**
- [ ] **6.2.3** 按住 Alt 删除则保留空隙
- [ ] **6.2.4** 片段边缘可拖拽调整入点/出点（**裁剪模式**，与 6.3 的变速模式区分）
- [ ] **6.2.5** 吸附：吸附到相邻片段边缘、播放头、时间轴起点；按住 Alt 临时关闭
- [ ] **6.2.6** 分割位置正好在边界时不执行并提示
- [ ] **6.2.7** 时间轴为空时显示"把素材拖到这里开始剪辑"
- **判据**：分割后前后两段时长之和等于原时长；删除后总时长正确减少

### 6.3 ⚠️⚠️ 变速（时间重映射）—— 本项目最核心的功能

> **产品所有者原话：「在时间轴上点击视频素材左右的拖动，让视频 10s 逐渐减到 1s，1s 又可以拉到 10s」。**
> **关键语义：拖边缘改变的是速度，不是裁剪。源时长不变，变的是 speed。**

#### 6.3.1 数据与交互

- [ ] **6.3.1.1** `Clip` 结构加入 `speed` 字段，`timelineDurationSec = (outPoint - inPoint) / speed` 由计算得出，**不单独存储**（避免数据不一致）
- [ ] **6.3.1.2** 拖片段**右边缘**：向右拖长 → 变慢；向左拖短 → 变快
- [ ] **6.3.1.3** 拖片段**左边缘**：改变起点与占用长度，同样改变速度（入点不变，调整 outPoint 与 speed 的组合）
- [ ] **6.3.1.4** ⚠️ **拖拽过程中实时显示当前倍速浮标**（如 `0.5x` / `2.0x` / `10.0x`）
- [ ] **6.3.1.5** ⚠️ 速度范围 `0.1x ~ 20.0x`，超出时边缘停住并显示"已达最慢/最快"
- [ ] **6.3.1.6** ⚠️ **两种模式可切换**（因为有时用户拖边缘是想裁剪）：
  - 片段左上角小图标切换"变速模式"与"裁剪模式"
  - **默认是变速模式**
  - 切换后图标和鼠标光标都要变化，避免误操作
- [ ] **6.3.1.7** speed 恰好为 1.0 时不加任何变速滤镜
- [ ] **6.3.1.8** 拖拽使片段与相邻重叠时推到不重叠（或按 Alt 允许覆盖）

#### 6.3.2 变速的后端实现（必须正确）

- [ ] **6.3.2.1** 视频用 `setpts`，系数 = `1/speed`
  - 10 倍速 → `setpts=0.1*PTS`；0.1 倍慢放 → `setpts=10*PTS`
- [ ] **6.3.2.2** ⚠️ **写 `buildAtempoChain(speed: number): string` 纯函数**
  - 规则：`speed == 1` 返回空串；循环中剩余因子 > 2.0 取 2.0、< 0.5 取 0.5；最后一个因子落在 [0.5, 2.0]；因子保留 2 位小数
  - 例：10 → `atempo=2.0,atempo=2.0,atempo=2.5`；0.1 → `atempo=0.5,atempo=0.5,atempo=0.4`
  - ⚠️ **必须写单元测试**，输入覆盖：0.1、0.2、0.25、0.5、0.75、1、1.5、2、3、4、5、10、15、20
  - **判据**：全部用例通过，且每个用例的因子乘积等于 speed（误差 < 1e-6）

- [ ] **6.3.2.3** 音频模式（片段级）：`保留原声并变速`（默认）/ `静音` / `保留原声并保持音调`
- [ ] **6.3.2.4** "保持音调"用 `rubberband=tempo=<speed>`；**先用 `ffmpeg -filters` 检测是否存在**，不存在则降级为 atempo 并提示用户
- [ ] **6.3.2.5** 片段无音轨但用户选了保留原声 → 静默处理不报错

#### 6.3.3 极慢放的画面卡顿处理

- [ ] **6.3.3.1** ⚠️ speed < 0.5 时界面主动提示"画面可能不流畅，建议先补帧再慢放"
- [ ] **6.3.3.2** 提供一键"补帧后慢放"，自动生成"先补帧再慢放"的任务链
- [ ] **6.3.3.3** 兜底方案：`minterpolate` 光流插值
  - ⚠️⚠️ **顺序必须是 `minterpolate` 在 `setpts` 之前**（先插帧到高帧率，再拉长时长）。反过来会插出一堆重复帧
  - ⚠️ 极慢（1080p 约 1~5 fps）且对快速运动有果冻伪影，**默认不启用**，只在用户明确选择"用 CPU 补帧"时使用
- **判据**：
  - 10 秒视频拖成 1 秒 → 导出 mp4 时长 1 秒、内容是完整 10 秒内容
  - 1 秒视频拉成 10 秒 → 导出 mp4 时长 10 秒
  - 音频在 2 倍速与 0.5 倍慢放时与画面对齐

### 6.4 贴图叠加（PRD F4）

- [ ] **6.4.1** 贴图轨拖入图片素材创建贴图层
- [ ] **6.4.2** ⚠️ **在预览画面上直接拖动贴图调位置、拖角调大小；位置以归一化坐标（0~1）存储**
  - 这样换分辨率不跑位
- [ ] **6.4.3** 参数：透明度、生效时间范围（相对片段起点）、淡入淡出时长
- [ ] **6.4.4** 多个贴图可叠加，按 z 顺序渲染
- [ ] **6.4.5** 贴图时间范围超出片段长度时自动截断
- [ ] **6.4.6** 后端用 `overlay` + `enable='between(t\,s\,e)'`
  - ⚠️ 逗号必须转义为 `\,`——**即使走 `-filter_complex_script` 文件也要转义**（文件方式解决的是命令行引号问题，不解决滤镜语法里的逗号分隔问题）
- [ ] **6.4.7** 淡入淡出用 `format=rgba,fade=in:st=<s>:d=<d>:alpha=1`
- **判据**：叠一个贴图在第 3~8 秒、右下角，导出后逐秒检查位置和时间都对

### 6.5 调色（PRD F5）

- [ ] **6.5.1** 片段级调色面板：亮度、对比度、饱和度、伽马、色温、色调滑块
- [ ] **6.5.2** 预设："原片""明亮""电影感""暖调"
- [ ] **6.5.3** 导入 `.cube` LUT 文件到工作区 `luts/`
- [ ] **6.5.4** ⚠️ **实时预览用 CSS filter 近似**，并标注"预览为近似效果，导出为精确效果"
- [ ] **6.5.5** 一键应用到所有片段
- [ ] **6.5.6** 后端映射：
  - 亮度/对比度/饱和度/伽马 → `eq=brightness=B:contrast=C:saturation=S:gamma=G`
  - 色温/色调 → `colorbalance`（按色温色调换算各通道偏移）
  - LUT → `lut3d=file=<路径>`
  - ⚠️⚠️ **LUT 路径在 Windows 上必须用正斜杠**（`file=C:/Canvora/luts/x.cube`）。反斜杠在 ffmpeg 滤镜语法里是转义字符，会导致**静默失败**
- [ ] **6.5.7** LUT 文件损坏时报错并继续导出，不因一个 LUT 让整个导出失败
- [ ] **6.5.8** 所有调色参数都是默认值时不加任何调色滤镜
- **判据**：调到明显偏暖，导出画面确实偏暖且与预览方向一致；套用 LUT 后导出有对应风格

### 6.6 播放头与预览（PRD F6）

- [ ] **6.6.1** 预览窗口按序列宽高比显示，黑边填充
- [ ] **6.6.2** 播放基于 proxy；多轨道用多个 `<video>` 同步播放，或按需切换 `src`
- [ ] **6.6.3** ⚠️ **预加载下一个片段的 proxy**，减少跨片段时的卡顿
- [ ] **6.6.4** 快捷键：空格播放/暂停、`←/→` 前后一帧、`Shift+←/→` 前后一秒、`Home/End`
- [ ] **6.6.5** 常驻小字提示"预览使用低清代理，导出为原画质"
- [ ] **6.6.6** proxy 未就绪时显示"正在准备预览文件…"及进度
- [ ] **6.6.7** proxy 生成失败时提示但仍允许导出（导出用原片，不需要 proxy）
- **判据**：3 段视频的序列连续播放不中断超过 0.5 秒

---

# 阶段 7（P1 第三部分）：滤镜图编译器与导出

> 目标：把编辑状态变成一条正确的 ffmpeg 命令，导出成片。

### 7.1 滤镜图编译器（PRD 10.1）⚠️ 最需要测试覆盖的模块

- [ ] **7.1.1** 定义 EDL 结构（`Timeline` + 素材绝对路径解析结果），前端提交 EDL 而非 `Timeline`
- [ ] **7.1.2** 校验：片段是否首尾相接、`inPoint < outPoint`、speed 在 0.1~20、贴图时间窗在范围内、所有素材文件存在
- [ ] **7.1.3** 每个片段生成视频链，**顺序必须正确**：
  ```
  trim → setpts=PTS-STARTPTS（时间归零）→ scale/pad（统一尺寸）→ setpts（变速）→ 调色
  ```
- [ ] **7.1.4** 每个片段生成音频链：`atrim → asetpts=PTS-STARTPTS → atempo 链 → aresample=48000`
- [ ] **7.1.5** ⚠️ **无音轨的片段必须用 `anullsrc` 补静音**，否则 `concat` 会失败（常见坑）
- [ ] **7.1.6** 拼接：`[v0][a0][v1][a1]... concat=n=N:v=1:a=1 [vcat][acat]`
- [ ] **7.1.7** 贴图叠加在拼接之后，`enable` 窗口按**时间轴绝对时间**计算
- [ ] **7.1.8** 输出映射：`-c:v libx264 -crf 18 -preset medium -pix_fmt yuv420p -c:a aac -b:a 192k -movflags +faststart`
- [ ] **7.1.9** ⚠️⚠️ **滤镜图写入临时 .txt 文件，用 `-filter_complex_script` 传，不从命令行拼**
  - 原因：多片段时滤镜文本会超过 8KB，命令行有长度上限；且能避开 Windows 转义地狱
- [ ] **7.1.10** ⚠️ **必须写单元测试**：给定构造好的 EDL，断言生成的滤镜图字符串包含：
  - 正确的片段数量
  - 正确的 `setpts` 系数
  - 正确的 `atempo` 链
  - 正确的 `between` 时间窗与转义
  - **判据**：全部通过

### 7.2 导出成片（PRD G1）

- [ ] **7.2.1** 导出设置：文件名、输出目录、分辨率（1080p/1440p/4K/跟随源）、帧率、CRF（默认 18，给出"体积/画质"说明）、编码器
- [ ] **7.2.2** ⚠️ 若检测到 NVIDIA 显卡，提供 `h264_nvenc` 硬件编码选项并标注"更快但画质略低"
- [ ] **7.2.3** ⚠️ **用 `-progress pipe:1` 解析结构化进度**，显示百分比、已用时间、预估剩余、当前帧率与码率
- [ ] **7.2.4** 导出完成：显示"导出完成"+"打开所在文件夹"+"在时间轴中回看"，成品加入素材库
- [ ] **7.2.5** ⚠️ **导出前检查全部素材可用性**，列出缺失项并拒绝导出（不要导到一半才失败）
- [ ] **7.2.6** 开始前估算文件大小并检查磁盘空间
- [ ] **7.2.7** ⚠️ 分辨率不一致的片段统一 `scale` + `pad`（保持比例不拉伸，加黑边）
- [ ] **7.2.8** 帧率不一致的统一到导出帧率
- [ ] **7.2.9** ⚠️ 失败时把完整 stderr 存到 `exports/<name>.ffmpeg.log`，界面显示中文摘要 + "查看详细日志"
- [ ] **7.2.10** 取消导出时删除不完整的输出文件
- **判据**：3 段不同分辨率不同帧率的视频拼接导出，播放正常、音画同步、时长误差 < 0.1 秒

### 7.3 导出首帧/尾帧（PRD G2）

- [ ] **7.3.1** 两个入口：视频节点右键菜单；时间轴导出面板
- [ ] **7.3.2** 输出格式 PNG 或 JPG（可选质量）
- [ ] **7.3.3** ⚠️ 尾帧不要只用 `-sseof -0.1`（受关键帧对齐影响，可能取不到绝对最后一帧）
  - **更可靠做法**：先用 ffprobe 取时长 D，再用精确 seek（`-ss <D-0.04>` 放在 `-i` 之后）取帧
- [ ] **7.3.4** 导出后作为素材入库并在画布创建图片节点
- **判据**：与视频实际首尾帧一致（用图像查看器逐帧核对）

### 7.4 导出预设（PRD G3）

- [ ] **7.4.1** 保存/删除/命名导出预设
- [ ] **7.4.2** 内置 3 个预设：抖音竖屏 1080x1920、B站横屏 1920x1080、高画质 4K
- **判据**：保存预设后重开应用仍在；选择后参数被正确填充

---

# 🚩 P1 验收关口

- [ ] **走通 PRD 2.4 场景三**：3 段素材 → 分割删除 → 变速 → 贴图 → 调色 → 导出成片
- [ ] 执行 PRD 12.5 / 12.6 的全部验收项
- [ ] `buildAtempoChain` 与滤镜图编译器的单元测试全部通过
- [ ] 导出后 `temp/` 目录为空；任务管理器无残留子进程

**此时 Canvora 已经是"产品所有者真正要的东西"。P2 是加分项。**

---

# 阶段 8（P2）：打磨与增强

> 每一项独立，可按价值排序做，不必全部完成。

### 8.1 可靠性

- [ ] **8.1.1** 任务队列持久化完善：应用崩溃后重启能正确恢复状态
- [ ] **8.1.2** 断点续跑：视频放大中断后能从已完成的批次继续，而不是从头
- [ ] **8.1.3** 退出时清理残留子进程；任务中心提供"清理残留进程"按钮
- [ ] **8.1.4** 启动时清理上次遗留的 `temp/` 目录

### 8.2 效率

- [ ] **8.2.1** 批量处理：一次选中 10 张图全部放大
- [ ] **8.2.2** proxy 预生成策略优化（导入时立即生成，而不是用到才生成）
- [ ] **8.2.3** 快捷键自定义
- [ ] **8.2.4** 画布在 200 节点以上的性能优化（虚拟化、LOD）

### 8.3 功能增强

- [ ] **8.3.1** 转场特效（叠化、黑场过渡）
- [ ] **8.3.2** 音频增强：背景音乐轨道、音量调节、淡入淡出
- [ ] **8.3.3** 提示词库：可保存常用提示词并复用
- [ ] **8.3.4** 首尾帧的图床配置入口（针对只接受 URL 的接口）

### 8.4 可选：Tauri 外壳

- [ ] **8.4.1** 装 Rust 工具链 + MSVC Build Tools（3~7 GB）
- [ ] **8.4.2** 把前端 `dist` 指给 Tauri；把后端作为 sidecar 或进程内服务
- [ ] **8.4.3** ⚠️ 用 `convertFileSrc()` 处理本地媒体路径，直接用 `C:\...` 会被 WebKit/WebView2 拦
- [ ] **8.4.4** ⚠️ sidecar 二进制必须带 `-x86_64-pc-windows-msvc.exe` 后缀，否则报 `os error 2`
- [ ] **8.4.5** 配置 asset protocol 与 CSP（需含 `asset: http://asset.localhost`）
- [ ] **8.4.6** ⚠️ 退出时杀整个进程树（`taskkill /F /T /PID`）
- **判据**：双击 exe 就能打开，功能与网页版一致

### 8.5 可选：启用 libplacebo（Anime4K）

- [ ] **8.5.1** 先验证当前 ffmpeg 是否支持：`ffmpeg -h filter=libplacebo`
  - ⚠️ gyan.dev 的 **essentials 构建确定不含** libplacebo（它依赖 Vulkan）
- [ ] **8.5.2** 换用 BtbN 的静态构建（其构建脚本确认启用 `--enable-libplacebo`）
- [ ] **8.5.3** 加 Anime4K shader 支持
  - ⚠️ 仅在动画内容上推荐，实拍会失真

---

# 附：跨阶段的持续性要求

以下事项**从第一个提交开始就要做**，不是某个阶段的任务：

- [ ] **持续 1** 每新增一个第三方依赖，都要读它的 `LICENSE` 文件本身（不要信 README 里的"open source"），并登记到 `DEPENDENCIES.md`
  - ⚠️ GitHub 侧栏显示 `NOASSERTION` 或 `Other` 是危险信号，必须人肉读
- [ ] **持续 2** 每完成一个可独立使用的功能，做一次中文提交
- [ ] **持续 3** 每个提交前自测：跑一遍相关的手动测试，确认没破坏已通过的功能
- [ ] **持续 4** 发现 PRD 有遗漏或矛盾，用 `// TODO(PRD-MISSING):` 标注并在交付说明里列出，**不要默默自行决定**
- [ ] **持续 5** 所有面向用户的文字都用简体中文，错误提示必须是人能看懂的（不是英文异常堆栈）
- [ ] **持续 6** 所有子进程调用用参数数组，不拼 shell 字符串
- [ ] **持续 7** 所有大文件路径来自设置，不硬编码 `C:` 盘
