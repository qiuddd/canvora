# Canvora 回归记录

> 记录每一次真实验证的结果。**没有实际跑过的项目不要勾选。**

## 2026-09-13 第八轮（v0.0.3：剪辑播放内核重写、片段拖动、贴图/调色 UI）

### 自动化检查

- [x] `npm run typecheck`（frontend + backend + shared）通过
- [x] `npm run build` 前端构建通过；`npm run build --workspace backend` 通过
- [x] 后端测试 **70 个全部通过**（新增：HTTP Range 解析 3 个、图片输入 `-loop 1` 2 个、贴图循环时长钳制 1 个）

### 根因修复验证（浏览器实测 + ffmpeg 取证）

- [x] **素材文件接口支持 Range**：修复前 `video.seekable` 只有 `[[0,0]]`（暂停定位后 `currentTime` 永远弹回 0）；修复后同一接口返回 `206 Partial Content`、`seekable=[[0,10]]`，暂停时定位到 5s/8s，`video.currentTime` 精确等于播放头
- [x] **播放帧级同步**：点击播放 2.28s 时 `video.currentTime=2.28`、播放头 136.2px（=2.28s×60 缩放）；再播 2s 后 timecode 00:04.2 / video 4.3s，偏差 < 0.1s
- [x] 播放到时间轴终点自动停止（按钮从「暂停」恢复为「播放」）
- [x] 从 8s 处起播 2s 后 timecode 00:09.9 / video 9.98，中途起播不失步
- [x] 空格键 = 播放/暂停（按钮文案与 `video.paused` 双向确认），画布平移不再抢空格

### 剪辑基础操作（浏览器实测）

- [x] 标尺点击定位到 5s；按住标尺从 3s 拖到 8s，播放头与画面同步跟随
- [x] 按住片段主体拖动 120px（zoom=60 → 2s），`startAt` 0→2，保持选中
- [x] 播放头 5s 分割片段 2~12 → 两段 [2~5(源0~3)] [5~12(源3~10)]，入出点换算正确
- [x] Delete 删除选中片段，后面的片段前移补齐（5→2）
- [x] 加第二条视频轨后把片段跨轨拖过去（合成指针事件验证），片段换轨、位置保持

### 贴图与调色（浏览器实测）

- [x] 贴图轨图片片段带默认布局（x 0.66 / y 0.08 / 宽 28%），预览画面按 contain 显示矩形正确叠加
- [x] 在预览里拖动贴图：x 66%→46%、y 8%→31.7%，DOM 位置与 store 布局一致
- [x] 亮度滑杆拉到 0.5：`colorGrade.brightness=0.5`，预览 `style.filter=brightness(1.5)`
- [x] 导出 EDL 检查：贴图时间窗 2~7s 换算成成片输出时间挂到主轨片段，`kind: video/image` 标记齐全

### 导出取证（ffmpeg 实测成片）

- [x] 导出「视频 3~10s」：成片 7.000s，h264 1080p + aac，音视频流时长一致
- [x] 发现并修复「贴图循环比主片长 → 成片尾部多 0.5s 冻结帧」（7.0 → 实测 7.5），钳制后复测 12.000s、视频流 = 音频流 = 12.000s
- [x] 综合导出「视频(带贴图+调色) 0~7s + 主轨图片片段 7~12s」：t=4s 贴图中心像素 `fc0000`（红 logo，与预览位置一致）；t=1s 同位置 `0000fe`（背景蓝，窗口外无贴图）；t=8s 画面中心 `fc0000`（图片片段按 `-loop 1` 循环渲染、pad 居中）——主轨图片片段音画严格对齐

### 测试数据清理

- [x] 测试项目「剪辑回归测试」与素材、导出产物已从工作区删除（5 个文件 14.2MB）
- [x] 临时素材目录已删除，测试用前后端进程已停止

## 2026-09-11 第七轮（v0.0.2：拖拽语义、生成节点、后台管理）

### 自动化检查

- [x] `npm run typecheck`（frontend + backend + shared）通过
- [x] `npm run build` 前端构建通过；`npm run build --workspace backend` 通过
- [x] `npm test` 后端 **64 个测试全部通过**（新增 5 个生成适配器测试）

### 修复验证（浏览器实测）

- [x] **拖标题栏不再复制**：拖动某个素材节点标题栏 130px，节点 left 从 183.262 变为 313.262（正好 +130），节点数保持 6 个不变
- [x] 节点根元素已无 `draggable` 属性（`getAttribute('draggable')` 返回 null）
- [x] 拖到时间轴的独立把手仍可用：对 `.drag-to-timeline` 派发 dragstart 后 `dataTransfer` 里能取到素材 id
- [x] 「⇱ 时间轴」把手在视频节点标题栏正常显示

### 生成链路（本地 mock 服务商，真实调用）

用本地 OpenAI 兼容图像服务（`scripts/mock-provider.mjs`）在 F:/Canvora 创建了一个临时服务商，全流程走通后已删除：

- [x] 生图节点自动列出已配置服务商与模型（下拉显示「本地回归测试 · 回归测试图片」），同时显示尺寸（1024x1024 等）与张数选择
- [x] 没有配置服务商时节点显示「还没有可用的服务商 / 请到后端管理页添加服务商和密钥」，并给出后台地址
- [x] 输入提示词后点「生成图片」→ 提示「图片已生成并放回画布」
- [x] 画布节点数 7 → 8：生成节点下方自动出现结果节点
- [x] 结果图片真实可显示：`/api/assets/<id>/file` 加载成功，natural size 1024×1024，素材名「AI 生成图片.jpg」
- [x] 测试完成后删除临时服务商与测试素材，服务商数归 0，密钥记录只剩用户自己的 DeepSeek 一条
- [x] 生成的测试节点已从画布删除，项目恢复原有 6 个节点 / 1 条连线

### 后台管理页（浏览器实测）

- [x] `/` 返回 200，包含五个页签：概览、服务商、项目、素材、任务
- [x] 服务商页有「从预置添加」下拉，7 个预置全部出现（DeepSeek Flash、阿里百炼图片/视频、智谱图片/视频、火山引擎、MiniMax）
- [x] 有自定义服务商表单：名称、协议、Base URL、模型（模型ID:能力）、API Key
- [x] 项目页有新建、重命名、删除按钮；素材页有重命名、删除；任务页有取消、删除、移除
- [x] 密钥输入框是 password 类型，页面只显示尾四位与测试状态，没有任何明文回显

### 接口层

- [x] `DELETE /api/providers/:id` 会同时删掉该服务商的密钥记录（实测删除后密钥数从 2 回到 1）
- [x] `DELETE /api/tasks/:id`、`DELETE /api/jobs/:id`、`PATCH /api/assets/:id` 已提供并被后台页使用
- [x] 生成接口新增能力校验：停用服务商、模型不存在、模型能力不匹配都会用中文拒绝

### 仍未完成或未实测

- [ ] 阿里百炼 / 智谱真实账户的生图与生视频联调（需要对应服务商密钥）
- [ ] 云端视频任务在应用重启后自动恢复轮询
- [ ] 时间轴贴图与调色的界面入口
- [ ] 音频模式「保持音调」仍会降级为 atempo
- [ ] 时间轴片段横向拖动位置
- [ ] `.canvora` 画布导入导出、SQLite、画布小地图与对齐辅助线
- [ ] `h264_nvenc` 只做了参数级单测，未真机导出

## 2026-09-11 第六轮（v0.0.1 云端生成后端）

### 自动化检查

- [x] `npm run typecheck`
- [x] `npm run build`
- [x] `npm test`：后端 59 项通过
- [x] 仓库扫描：未跟踪 `secrets.json`、未发现 API key 字符串

### 本地 mock 生图回归

- [x] 配置 OpenAI 图像协议 Provider，密钥只进入加密工作区
- [x] `POST /api/generation/image` 返回成功
- [x] mock 返回的 base64 图片下载到项目 `assets/`
- [x] 生成文件登记为素材，带尺寸、哈希和来源
- [x] 未把图片上传到公共图床

### 尚未完成或未实测

- [ ] 阿里百炼真实生图、生视频账户联调
- [ ] 智谱真实生图、生视频账户联调
- [ ] 云端任务重启后继续轮询
- [ ] 生成节点自动在画布创建结果节点
- [ ] 前端完整 Provider 设置面板



### 自动化检查

- [x] `npm run typecheck`（frontend + backend + shared）通过
- [x] `npm run build` 前端构建通过；`npm run build --workspace backend` 通过
- [x] `npm test --workspace backend`：**59 个测试全部通过**

### 本地 AI 工具（走 7897 代理真实下载并实测）

代理实测：`http://127.0.0.1:7897` 可访问 github.com（HTTP 与 socks5h 都通），7890 拒绝连接。

- [x] `scripts/fetch-tools.mjs` 增加 `--proxy=`（默认 `http://127.0.0.1:7897`，`--no-proxy` 可关），走 `curl --proxy`
- [x] 用脚本重新下载并安装成功：「下载完成，共 43.4 MB」→ `F:\Canvora\bin\realesrgan-ncnn-vulkan.exe` + `models` 就位
- [x] RIFE 20221029 装到 `F:\Canvora\bin\rife-ncnn-vulkan.exe`（含 rife-v4.6 等模型）
- [x] `GET /api/tools` → `{ffmpeg:true, ffprobe:true, realesrgan:true, rife:true}`
- [x] **图片放大真机验证**：`小猫咪.jpg` 754×882 → 用 `realesrgan-x4plus -s 4 -t 128` 输出 **3016×3528**（PNG 9,450,194 字节），尺寸与预期完全一致
- [x] **视频补帧真机验证**：26.215 秒 / 30fps 素材 → 输出 **26.2 秒 / 60fps / 1572 帧**（源 786 帧 ×2）

### 修掉的 bug

1. **补帧只出 0.13 秒残片（严重）**：`buildRifeArgs` 把 `-n` 当倍率传，但 RIFE 的 `-n` 是**目标总帧数**。
   传 `-n 2` 时它只输出 2 帧左右 → 成片 0.133 秒 / 8 帧。改成传「这一批输入帧数 × 倍率」后实测 1572 帧、26.2 秒。
   单测同步改成断言目标总帧数语义。修复前产生的坏产物已从素材库删除。
2. **对话"不能读图"是我的误判**：上一轮我用 `max_tokens: 60` 测试，模型把预算全花在 `reasoning_content` 上，
   `content` 为空就误以为不支持 vision。查官方文档确认 `deepseek-flash` 支持图片，去掉 token 限制后实测能准确读出画面内容。
   后端现在把 `finish_reason=length` 区分开来并给出「回答被长度截断，请重发」的中文提示，不再误报。
3. **时间轴剪辑语义纠正**：产品所有者指出拖边缘应该是**裁剪**而不是变速。已把默认模式翻转为裁剪，
   变速改为双击/右键显式切换，徽标区分「保留 6.2s」与「4.22x」。
4. **检查器提示没跟着默认值走**，仍显示「变速模式」——已修。

### 浏览器实测（内置浏览器实际操作）

- [x] 时间轴出现**多条轨道**（视频轨 1、贴图轨 1），可「＋视频轨 / ＋贴图轨」新增
- [x] 轨道上的片段右键弹出 7 项菜单：在播放头处分割 / 复制片段 / 删除并前移 / 删除并保留空隙 / 静音这个片段 / 导出时跳过这段 / 切换到变速模式
- [x] **拖右边缘向左 1200px → 源时长从 26.21 秒变成 6.21 秒**（只保留 0~6.21 秒），时间轴总长同步变 6.2 秒 —— 这就是「从右往左拉=裁掉」的正确语义
- [x] **空格 + 拖动平移画布**：位移 -200/-100，且没有误触发框选
- [x] 图片/视频节点现在可拖到时间轴轨道（节点带 `draggable` 并携带素材 id）
- [x] **对话上传图片真机验证**：拖入一张 103,605 字节的 JPEG → 附件缩略图显示 → 发送 → DeepSeek 返回真实画面描述「雨夜霓虹小巷里，一名握发光长刀的女战士正与浑身暗红纹路的巨型怪人当街对峙」
- [x] 深色模式对比度加强（连线加粗、选中描边改浅紫、端口加轮廓、时间轴片段改亮色渐变、播放头加发光）

### 后台与启停

- [x] 后端状态页 `http://127.0.0.1:8787` 返回 200，含服务状态、工作区、版本、运行时长、项目/素材数、**内存占用**（实测 115 MB / 堆 18 MB）、四个工具就绪状态、最近 20 条任务、项目列表、关闭按钮
- [x] 有任务在跑时状态页每 3 秒自动刷新
- [x] `启动.bat`：检查依赖 → 提示工具缺失 → 起后端 → 轮询 `/api/health` 等就绪 → 起前端 → 自动开浏览器
- [x] `关闭.bat`：先 `POST /api/shutdown` 让后端优雅退出，再按端口 8787/5173 杀进程树，最后清理 ffmpeg / realesrgan / rife 残留进程

### 仍未完成 / 未验证

- [ ] **贴图叠加与调色的界面入口**：后端滤镜图支持 overlay / eq / colorbalance / lut3d 且已实测（LUT 生效、PSNR 12.9dB），但画布上还没有拖贴图和调色滑块的 UI
- [ ] **音频模式的"保持音调"**：需要 ffmpeg 的 rubberband，本机未验证，选了会降级为 atempo
- [ ] 时间轴片段横向拖动位置（目前只能删除后重拖）
- [ ] 图片作为片段时后端滤镜图是否按静帧处理，未单独验证
- [ ] 云端生图 / 生视频适配器
- [ ] `.canvora` 画布导入导出
- [ ] SQLite（仍是 `canvora-state.json`）
- [ ] 画布 100 节点性能、视口裁剪、小地图、对齐辅助线
- [ ] `h264_nvenc` 只做了参数级单测，未真机导出
- [ ] 深色模式的对比度只是改了配色，没有做无障碍对比度测量
