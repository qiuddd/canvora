# 更新记录

## v0.0.3 — 2026-09-13

### 修复（剪辑播放不能用的根因）

- **素材文件接口不支持 HTTP Range，视频预览永远无法定位画面**：后端原本把文件裸流式输出（无 `Accept-Ranges`、无 `Content-Length`），浏览器把视频当成不可 seek 的流（实测 `seekable` 只有 `[[0,0]]`），拖进度条、暂停定位全部失效，画面停在开头。现在 `/api/assets/:id/file` 支持 `Range: bytes=...` 分段传输（206 Partial Content），解析逻辑有独立单测。
- **播放时播放头与视频互相拉扯**：预览重写为混合时钟模型——视频片段在播放时以视频时钟推进播放头（帧级同步，实测 2s 处偏差 < 0.1s），变速超出预览能力（<0.25x 或 >4x）或非视频片段时按真实时间推进并周期纠偏；暂停时把画面 seek 到播放头（容差 0.02s）。
- **跨片段播放黑屏**：原来每个片段的 `<video>` 用 key 强制重挂载，新元素元数据没加载完就 seek 被吞掉。现在单个 video 元素只换 src，元数据加载完成后再定位到目标时间。
- **图片片段预览是坏的**：图片被塞进 `<video>` 标签无法显示，现在图片片段用 `<img>` 预览。
- **片段静音在预览不生效**：`muted` 原来写死 false，现在跟随片段的音频模式。
- **导出成片尾部多冻结帧**：贴图图片循环输入比主片长时，overlay 滤镜跟随最长输入，尾部多出一段冻结帧（实测成片 7.0s 变 7.5s）。贴图循环时长现在以成片总长为上限，实测导出音视频流时长精确一致。
- **主轨图片片段导出音画错位**：图片输入没有 `-loop 1`，整段只有 1 帧，与静音音频拼接后音画错位、贴图淡入淡出整段透明。现在图片片段（含贴图输入）按 `-loop 1 -framerate <fps> -t 时长` 输入，实测导出音画严格对齐。
- **空格 / Delete 快捷键冲突**：画布「按住空格平移」和时间轴「空格播放」、删除节点和删除片段同时触发。现在时间轴展开时空格交给播放/暂停，Delete 优先删选中的片段。
- **时间轴长了没法滚动**：刻度和片段都是绝对定位，撑不开横向滚动区域，超出可视区的片段看不到。现在滚动内容有显式宽度，轨道标签吸附在可视区左缘，播放时自动跟滚。

### 新增

- **片段拖动**：按住片段主体可拖动位置，支持跨同类型轨道，吸附到轨道起点 / 播放头 / 其他片段边缘，落点就近落进空隙、绝不与已有片段重叠。
- **贴图轨真正可用**：拖图片到贴图轨，在预览画面里直接拖动定位，检查器可精调横向/纵向/宽度/不透明度/淡入/淡出；导出时贴图自动换算成与主轨片段重叠的叠加层（时间窗换算到成片输出时间，空白被剪掉也不会错位）。实测导出成片贴图位置与预览一致。
- **片段调色**：选中主轨片段出现亮度/对比度/饱和度/色温滑杆，预览用 CSS 滤镜近似，导出走 ffmpeg 的 eq/colorbalance（语义一致，可重置）。
- **标尺刷选**：按住标尺左右拖动连续改播放头。
- 导出前提示：主轨有空隙时提醒「导出按顺序拼接，空隙会被剪掉」；有非主轨片段时提醒只有第一条视频轨参与导出。

### 说明

- 后端 EDL 是顺序拼接（concat）模型：导出成片 = 主轨片段依序相连，片段之间的空隙会被剪掉；预览时空隙显示黑场。导出前会提示。
- 后端单测从 64 个增加到 **70 个**（Range 解析 3 个、图片输入与贴图时长 3 个）。

## v0.0.2 — 2026-09-11

### 修复

- **画布上移动素材会变成复制**：素材节点原本整个是 HTML5 可拖拽元素，和标题栏的指针拖动抢事件，拖标题栏时既移动又产生素材拖拽数据，落到画布就被当成新节点。现在节点整体不再可拖拽，标题栏只负责移动，拖到时间轴改用标题栏上独立的「⇱ 时间轴」把手。
- **生成节点让用户手填服务商 ID 和模型 ID**：改为从后台已配置的服务商里下拉选择，用户看不到也不需要知道内部 ID。
- 生成节点的项目归属：节点创建时写入当前项目 ID，打开旧画布时把 `local` 归一到当前项目，避免生成结果写进错误目录。
- 生成结果不回画布：生图多结果和视频异步任务完成后，会自动下载、登记素材，并在生成节点下方创建结果节点。

### 新增

- **本机后台管理页**（<http://127.0.0.1:8787>）从只读状态页升级为管理页面：概览、服务商、项目、素材、任务五个页签，支持增删改查、从预置添加服务商、测试连接、启停用、项目重命名/删除、素材重命名/删除、任务取消与记录清理。密钥只能录入或替换，页面永不回显明文。
- 生成参数选择：生图可选尺寸与张数，生视频可选比例与时长。
- 生成节点按模型能力过滤：生图只列图片能力的模型，生视频只列视频能力的模型，服务端二次校验。
- 参考图 / 首帧 / 尾帧通过输入口接入，后端读取项目内素材转成服务商要求的输入，不上传公共图床。
- 任务中心分开展示云端生成任务与本地任务，支持取消与清理。
- 新增生成适配器单测（能力过滤、停用服务商、模型能力不匹配、图片结果三种返回格式）。

## v0.0.1 — 2026-09-11

### 已完成

- 无限画布、项目入口、项目隔离和素材库。
- 图片、视频、音频拖拽导入与浏览器预览。
- 节点多选、框选、复制、打组、真实端口连线和类型校验。
- 图片分割、Real-ESRGAN 放大、RIFE 补帧、首尾帧导出。
- 多轨道时间轴、裁剪、显式变速、分割、预览和 ffmpeg 导出。
- DeepSeek Flash 文本和图片对话，图片按官方多模态格式发送。
- Provider 配置、密钥加密存储和统一云端生成入口。
- OpenAI 兼容生图、阿里百炼/智谱图片入口，以及阿里百炼/智谱视频异步任务入口。
- 云端生成结果下载到当前项目素材库并登记，服务商密钥不返回前端。
- 后端状态页、启动/关闭脚本和本地工具代理下载。

### 已知限制

- 服务商具体模型参数仍以官方接口版本和账户权限为准，需逐家联调。
- 视频云任务当前运行时轮询；应用重启后的云任务恢复、完整取消接口和生成结果自动新建画布结果节点仍待增强。
- Provider 配置面板仍需完善，当前可通过后端接口或节点中的服务商 ID/模型 ID 配置。
- 持久化仍为 JSON 文件，尚未迁移 SQLite。
- 时间轴贴图/调色完整 UI、画布小地图和 `.canvora` 导入导出尚未完成。
