# Canvora 产品需求文档（PRD）

| 项 | 内容 |
|---|---|
| 产品名称 | Canvora |
| 文档版本 | v1.0 |
| 编写日期 | 2026-09-11 |
| 文档状态 | 待开发 |
| 目标读者 | 负责实现的 AI 开发代理；以及不懂代码、需要验收的项目所有者 |
| 配套文件 | `TASKS.md`（分期任务清单）、`AGENTS.md`（项目硬性约定）、`README.md`（使用说明） |

---

## 1. 这份文档怎么用（开发代理必读）

### 1.1 阅读顺序

1. **先读第 2、3 节**：搞清楚产品要什么、明确不要什么。第 3 节（不做的事）和第 12 节（硬性约束）一样，是**约束**而非建议，不要自行扩展。
2. **再读第 5、6 节**：本机硬件约束和技术选型结论。第 6 节里每一条"被否决的选项"都附了否决理由，**不要因为"技术上更优雅"就改回被否决的方案**。
3. **然后读第 7、8 节**：架构和数据模型是一切实现的地基，先定这两块再写代码。
4. **最后按 `TASKS.md` 逐期实现**。`TASKS.md` 把第 9 节的功能需求拆成了可执行、可勾选的任务。

### 1.2 硬约束汇总（不可自行更改）

以下条目已在第 6 节给出理由，这里先汇总，方便随时回查：

- **禁止引入任何许可证不是 MIT / Apache-2.0 / BSD 的第三方库或二进制**。具体黑名单见第 6.4 节。
- **禁止引入 Python / PyTorch 运行时**。后端只用 Node.js + TypeScript。
- **禁止把 `<video>` 元素画进 `<canvas>` 来播放**。视频一律用 DOM 元素浮在画布上层。
- **禁止从命令行拼接 ffmpeg 滤镜字符串**。长滤镜图一律写临时文件走 `-filter_complex_script`。
- **禁止把窗口内所有视频元素同时挂载并播放**。必须有 LOD（细节层次）与离屏暂停策略。
- **禁止把 API 密钥写入任何会被提交到 git 的文件**。
- **所有大文件目录（模型、proxy 缓存、抽帧中间文件）默认指向 `F:` 盘或 `G:` 盘**，不得默认写 `C:` 盘。

### 1.3 遇到需求不明确时怎么办

- 优先在本文档内找答案；本文档对每个功能都写了"异常与边界"。
- 本文档确实没覆盖的，**选择开发量更小的那一版实现，并在代码注释里用 `// TODO(PRD-MISSING):` 标出**，然后在交付说明里列出。不要自行发明复杂功能。
- **不要引入本文档没有提到的第三方服务、SDK 或云平台。**

---

## 2. 产品定位

### 2.1 一句话说明

> Canvora 是一个**跑在自己电脑上的无限画布工作台**：把素材（图片、视频）摊在一块可以无限拖拽缩放的画布上，在画布上直接调用 AI 接口生成新素材，并把画布上的视频拖进时间轴做基础剪辑，最后导出一条成片。全部数据留在本地，不经过任何第三方服务器。

### 2.2 要解决什么问题

项目所有者（下称"用户"）当前的痛点，按优先级排列：

1. **市面上的无限画布工具把基础功能做成付费项**。用户需要的是一个完全免费、功能不受限、数据在本地、不需要登录的工具。
2. **AI 创作流程散落在多个网页之间**。生成一张图要开一个网站，放大要开另一个，生视频再开一个，素材下载到本地文件夹之后就散了，找不回来。用户希望这些步骤在**同一块画布上完成，并且能连起来自动跑**。
3. **AI 生成的素材画质不够**，需要本地放大到 4K；AI 生成的视频帧率/时长不理想，需要补帧和变速。这些目前没有顺手的本地工具。
4. **素材攒多了管不住**。桌面上的视频、图片文件越来越多，没有直观的方式浏览和挑选。

### 2.3 目标用户

**只有一个人：项目所有者本人。** 这是本 PRD 最重要的定位约束。

- 非程序员，不写代码，不懂技术术语。
- 使用 Windows 11 笔记本，单机使用，不需要多人协作、不需要云端同步、不需要账号体系。
- 需要把生成的图片/视频用于内容创作（AI 短片、动画、社交平台素材）。
- 愿意为了省钱接受"每次手动启动"和"界面不如商业软件精致"。

**由此推导出的设计取向：**

- **可用性优先于性能**：功能能跑通、结果正确，比跑得快更重要。
- **可靠优先于优雅**：失败时要给出人能看懂的中文提示，而不是抛技术错误。
- **本地优先**：任何需要联网的功能都必须能明确告诉用户"这一步在联网"，并允许失败后重试。
- **不需要考虑**：多用户、权限、协作、审计、国际化（只需简体中文界面）。

### 2.4 成功标准

P0 和 P1 全部完成后，用户应该能流畅完成以下三个场景，这是一切的验收基准：

**场景一：从一句话到一张 4K 图**
打开 Canvora → 在画布上新建"提示词"节点写一句话 → 连到一个"生图"节点 → 点运行 → 图片出现在画布上 → 点"放大 4 倍" → 得到 4K 图 → 存进素材库。

**场景二：从两张图到一条视频**
画布上放一张首帧图、一张尾帧图 → 新建"生视频"节点，把两张图连进去 → 选择首尾帧模式 → 点运行 → 生成的视频出现在画布上并可直接播放。

**场景三：从几段素材到一条成片**
把画布上 3 个视频拖进时间轴 → 把第 1 段中间不要的部分分割掉、删掉 → 把第 2 段从 10 秒拖成 3 秒（加速） → 在第 1 段上叠一个贴图并固定位置 → 统一调色 → 导出成一条 mp4。

**量化标准：**
- 场景一在 5 步操作内完成，全流程有明确进度提示，任一步失败都能重试而不丢失已达成结果。
- 场景三导出的 mp4 在系统自带播放器里能正常播放，音画同步，时长与时间轴显示一致，误差不超过 0.1 秒。
- 应用冷启动（双击启动脚本到浏览器出界面）在 10 秒内完成。

---

## 3. 明确不做的事（Out of Scope）

以下内容**本版本不做**。开发代理不得自行添加，也不得为它们预留复杂抽象。（列入此节是为了防止范围失控——很多项目死在"顺手也做了吧"。）

### 3.1 明确不做

| 不做的事 | 原因 |
|---|---|
| 多人协作、共享画布、实时同步 | 单用户本地工具，没有第二个用户 |
| 账号体系、登录、云端同步 | 数据只在本地；引入账号会把本地工具变成需要运维的服务 |
| 移动端 / 平板适配 | 只跑在用户的 Windows 笔记本上 |
| 国际化 / 多语言 | 只需要简体中文 |
| 接入国内需要企业资质、或需要代理+海外信用卡的 API 作为主路径 | 用户是个人开发者，拿不到企业资质；详见 6.5 节 |
| 本地部署大语言模型 / 文生图模型（如 ComfyUI、Ollama） | 用户机器 16GB 内存 + 8GB 显存，跑本地大模型会和剪辑任务抢资源；且已有云端 API 可用 |
| 3D 场景、粒子特效、关键帧动画曲线 | 属于专业剪辑软件范畴，与"基础剪辑"定位不符 |
| 音频的多轨混音、降噪、变声 | 本期只做"原声保留 + 跟随变速"这一档 |
| 转场特效库（叠化、划像等） | 本期只做硬切（直接拼接）；转场留到未来版本 |
| 自动字幕、语音识别 | 与核心流程无关 |
| 商业产品的打包分发、自动更新、安装程序 | 自用工具，不需要；且自动更新会带来安全问题（见 13.3 节） |

### 3.2 未来可能做，但本期不预留接口

- 自动更新机制
- 插件市场 / 远程插件下载
- 团队素材共享

**注意：** 尤其是"远程插件下载安装"这一类能力，本版本**不实现、不预留**。它需要执行下载来的代码，安全风险远大于收益。

---

## 4. 术语表

开发代理在代码命名和界面文案里统一使用以下术语，避免同义词混乱。

| 术语 | 英文 / 代码名 | 含义 |
|---|---|---|
| **画布** | Canvas | 一块可以无限拖拽、缩放的二维平面，用于摆放节点 |
| **节点** | Node | 画布上的一个方块，可以是文本、图片、视频、生成配置等 |
| **连线** | Edge / Connection | 两个节点之间的有向连接，表示"上游的数据喂给下游" |
| **上游 / 下游** | Upstream / Downstream | 沿连线方向，数据从上游流向下游 |
| **端口** | Port | 节点上的连线接入点，分输入口和输出口；连线必须表明"传的是提示词、图片还是视频" |
| **素材** | Asset | 一个被导入或生成的图片 / 视频文件，具有唯一 ID 和本地路径 |
| **素材库** | Asset Library | 所有素材的集合，可跨项目检索 |
| **项目** | Project | 一个工作单元 = 一块画布 + 一条时间轴 + 它的素材集合 |
| **工作区** | Workspace | 本地磁盘上存放所有项目数据和素材的根目录 |
| **序列** | Sequence | 时间轴上要导出成一条片子的完整内容（多段片段拼成） |
| **轨道** | Track | 序列中的一层，按类型分为视频轨、贴图轨、音频轨 |
| **片段** | Clip | 轨道上的一段，引用一个素材，带自己的入点/出点/时长/变速 |
| **入点 / 出点** | In / Out | 片段引用素材的起止时间位置（源时间），不是时间轴上的位置 |
| **时间重映射 / 变速** | Time Remap | 改变片段的播放速度，从而改变它在时间轴上占用的时长 |
| **代理文件** | Proxy | 从原素材生成的低码率小分辨率副本，只用于预览，不用于导出 |
| **滤镜图** | Filter Graph | ffmpeg 用来描述一串处理步骤的表达式 |
| **生成任务** | Generation Task | 一次提交给 AI 接口的异步生成请求，有状态和结果 |
| **首尾帧** | First/Last Frame | 生视频的一种模式：给首帧图和尾帧图，让模型生成中间过渡 |
| **LOD** | Level of Detail | 按缩放程度切换元素的渲染精度（如缩小时把视频换成静态缩略图） |

---

## 5. 硬件与环境约束

### 5.1 目标机器的实测配置（2026-09-11 实测）

| 部件 | 实测值 | 对设计的影响 |
|---|---|---|
| CPU | Intel i5-13500H，12 核 16 线程 | 软件编解码可行；CPU 光流补帧极慢，只作兜底 |
| 内存 | **16 GB** | **本机最硬的瓶颈**，见 5.2 |
| 显卡 | NVIDIA RTX 4060 Laptop，**显存 8 GB**（8188 MiB），计算能力 8.9 | 决定能跑多大的 AI 模型 |
| 驱动 | 610.47（支持 CUDA 12/13 运行时） | 不需要装 CUDA 工具包，运行时由驱动提供 |
| 系统 | Windows 11 家庭版中文版，Build 26200 | WebView2 已预装；WMIC 已被移除，**脚本里不要用 wmic** |
| 磁盘 | C: 剩 66.5 GB / D: 剩 51 GB / **F: 剩 288 GB / G: 剩 244 GB** | **所有大文件必须指向 F 或 G** |
| 已装 | Node v22.14、npm 10.9.2、Python 3.11.6、git 2.54、ffmpeg 7.1（gyan.dev essentials）、numpy、opencv | 可直接用 Node 和 ffmpeg |
| 未装 | pnpm、Docker、Rust、CUDA 工具包（新版）、PyTorch | **不要在设计里依赖 Docker** |
| 显示器 | 2560 x 1440 | 界面按 1440p 宽度设计，最小支持 1280 宽 |

### 5.2 由此推导的硬性限制

**限制 1：16 GB 内存是真正的瓶颈，不是 8 GB 显存。**

任何"把整个视频解码进内存"或"一次并行处理很多帧"的做法都会让机器卡死。必须：
- 视频处理一律走**流式/分块**，不整片载入内存。
- 本地 AI 任务**同时只允许跑 1 个**（无论 CPU 还是 GPU），任务队列串行执行。
- 前端不缓存大文件内容，媒体一律通过 URL 交给浏览器/播放器自己去流式读取。

**限制 2：C 盘只剩 66.5 GB，绝对不能让大文件落到 C 盘。**

必须做到：
- 工作区根目录**默认在首次启动时让用户选择**，默认建议 `F:` 或 `G:` 盘，绝不默认 `C:`。
- ffmpeg、Real-ESRGAN、RIFE 二进制和模型权重存放在工作区内的 `bin/` 和 `models/` 下。
- proxy 缓存、抽帧中间文件、导出文件全部在工作区内，且中间文件用完即删。
- 启动时检查工作区所在磁盘剩余空间，低于 20 GB 就在界面上给出黄色警告。

**限制 3：4K 视频抽帧会产生巨量临时文件。**

4K 一帧 PNG 约 20~25 MB，30fps × 60 秒 = 1800 帧 ≈ **45 GB**。必须：
- 中间帧一律用 **JPEG（`-q:v 2`）而非 PNG**，体积降约 10 倍，肉眼无差。
- **分块处理**：每批 100~300 帧，处理完立刻删除该批。
- 开始处理前估算所需空间并检查，不足则拒绝执行并给出明确的中文提示。
- 单次任务的临时文件总量设上限，超过则中止。

**限制 4：视频放大必须整段处理，不能预览式实时。**

用户不要期待"实时 4K 放大预览"。UI 上必须清楚说明这是后台批处理任务，会给出预计耗时。

**限制 5：不做 GPU 加速的视频编解码。**

用户已装的 ffmpeg 7.1 是 gyan.dev essentials 构建，**不含 libplacebo**（因此不能跑 Anime4K shader）。本版本：
- 视频放大走 Real-ESRGAN ncnn（Vulkan），不走 Anime4K shader。
- 编码统一用 `libx264`（软件），质量优先，速度可接受。
- 如果将来要上 libplacebo，需换成 BtbN 的静态构建，这是**未来可选项，本期不做**。

### 5.3 关键工具的获取方式（开发代理需要下载并随项目分发）

| 工具 | 用途 | 下载来源 | 许可证 | 说明 |
|---|---|---|---|---|
| ffmpeg + ffprobe | 所有视频处理 | gyan.dev essentials 构建（用户已装） | LGPL/GPL | 开发阶段直接用系统已装的；后续可换成项目内自带版本 |
| `realesrgan-ncnn-vulkan` | 图片/视频逐帧放大 | Real-ESRGAN 官方 release `v0.2.5.0`，资产名 `realesrgan-ncnn-vulkan-20220424-windows.zip` | **BSD-3-Clause** | 解压即用，含模型；**商用友好** |
| `rife-ncnn-vulkan` | 补帧 | nihui/rife-ncnn-vulkan 的 release（如在 20221029 及之后版本） | **MIT** | 解压即用，**商用友好** |
| Real-CUGAN ncnn | 动画向图片放大（可选） | nihui/realcugan-ncnn-vulkan release | **MIT** | 上线前需实测效果再决定是否加入 |

**安装脚本要求：** 提供一个 `scripts/fetch-tools.mjs`，从上述来源下载并解压到工作区 `bin/` 目录，校验文件存在与可执行。**不要把这些二进制提交到 git 仓库**（体积大且无必要），改用脚本获取 + 记录版本号。

---

## 6. 技术选型决策记录

每一节格式为：**决策 → 理由 → 被否决的选项及否决原因**。

### 6.1 起点：不 fork 任何项目，只借鉴设计与少量代码

**决策：Canvora 从零建立自己的代码库，不 fork 现有项目。**

**理由：**

调研发现两个同名但完全不同的项目，必须分清：

| 项目 | star | 许可证 | 技术栈 | 状态 | 结论 |
|---|---|---|---|---|---|
| `hero8152/Infinite-Canvas` | 3.0k | **自定义许可：禁止商用、二次开发必须开源并署名** | Python FastAPI + 原生 JS（无框架、无类型），786KB 单文件 | **作者已宣布停更（2026-08-28）** | **不可用作代码起点** |
| `basketikun/infinite-canvas` | 6.3k / 1.6k fork | **MIT**（已三处交叉核实：LICENSE 文件 main 与 master 分支、GitHub 侧栏、README 一致；版权行 `Copyright (c) 2026 basketikun`） | React 19 + TypeScript + Vite 7 + zustand + antd + Tailwind 4，纯前端无后端，自研 DOM 节点 + SVG 连线 | 活跃 | **可作为重要参考，但不直接 fork** |

**为什么连 MIT 那个也不直接 fork：**

1. **它是纯前端应用，数据存在浏览器 IndexedDB（localforage）里。** Canvora 必须有一个能调用 ffmpeg 和本地 AI 二进制的后端进程，素材必须是磁盘上的真实文件（用户要能用自己的播放器打开、要能放进别的软件用）。把它改造成"后端权威"，等于重写它的整个数据层。
2. **它面向的是"图片创作"**，视频只是生成结果的展示，没有时间轴、没有任何剪辑能力。时间轴和剪辑是 Canvora 的差异化核心，而它的画布代码与"网页/localhost + IndexedDB"耦合很深。
3. **它用的是 Bun 作为开发运行时**，用户机器上只有 npm。虽然可以改，但引入不必要的摩擦。
4. **它的许可证虽然是 MIT，但它是别人的项目。** 从零建立自己的代码库，结构可以完全按 Canvora 的需求设计，长期维护成本更低。

**可借鉴的部分（抄设计，不抄代码）：**
- **画布渲染方案：绝对定位的 DOM 节点 + 单层 SVG 画连线 + 一个祖先容器统一做 `transform: scale/translate`。** 这是行业验证的正确解法——节点卡片里有输入框、下拉框、滑块、视频播放器，这些用 DOM 天然免费；用 Canvas 库画则要自己造输入框、光标、滚动条，纯负收益。
- 节点类型清单与连线数据模型。
- 执行引擎思路：拓扑序（DFS 后序）遍历 + 环检测 + 按后端能力限流。
- 「前端框选、后端处理」的职责划分（裁剪、九宫格切分这类操作）。
- 视频节点右键"抽取首帧/尾帧/当前帧并生成图片节点"这个交互。

**被否决的选项：**

| 选项 | 否决原因 |
|---|---|
| fork `hero8152/Infinite-Canvas` | ①许可证禁止商用且要求二次开发开源，用户将来若想分享或收费就违法；②上游已停更，109 个未处理 issue；③Python + 786KB 无类型单文件，改造成本和重写相当 |
| fork `basketikun/infinite-canvas` | 见上述 4 条理由（纯前端 + IndexedDB + 无剪辑 + Bun） |
| 用 `tldraw` 作为画布 SDK | 技术上最合适（唯一自带视频 shape 的），但 **v4 起改为自定义许可：生产环境必须购买 license key，免费档强制水印且禁止商用，商业授权约 6k 美元/年量级**。与"不花钱"目标直接冲突 |
| 用 `Excalidraw` | MIT 免费，但没有视频元素类型，视频只能靠 embeddable/iframe hack，且它用 canvas 2D 渲染 + 叠加 DOM，播放器行为会和画布缩放打架 |
| 用 `Konva` / `react-konva` 作为画布 | 节点图的难点在"节点卡片内的富交互 UI"，不在图元渲染性能。Konva 上做输入框、滚动、输入法是纯负收益。**Konva 的正确用途是时间轴与预览合成层，本 PRD 在那里会用到** |
| 用 `React Flow` / `@xyflow/react` | MIT，但官方 issue 记录自定义节点会拖慢平移缩放，必须自己做 `React.memo` + LOD 优化。且它没有形状/资产/序列化系统，媒体管理仍要自建。**它的节点体验值得参考，但不作为依赖引入** |
| 用 `ComfyUI` 作为生成后端基座 | GPL-3.0 会传染；且它是工作流引擎而非素材画布，定位不符 |

### 6.2 交付形态：第一期做本地网页应用

**决策：一期交付为"本地网页应用"——启动脚本拉起本地服务，浏览器访问 `127.0.0.1:端口`。**

**理由：**
- 开发最快：改代码刷新即见效，无需编译等待，迭代成本最低。
- 无编译工具链门槛：用户机器上没装 Rust 和 MSVC Build Tools，走 Tauri 需要额外装 3~7 GB 工具链且首次编译 5~15 分钟。
- 跨设备可用：局域网内用手机/平板也能打开查看（这是附带好处，不做专门适配）。

**代价（必须在 README 里如实告知用户）：**
- 每次使用要先双击启动脚本，用完要关掉命令行窗口；不会像真软件那样有任务栏图标、不能开机自启。
- 浏览器对超长任务（几小时）的稳定性不如原生应用；因此所有长任务都在**后端**执行，前端只负责展示进度，即使关掉页面任务也不会中断。

**为将来留的门（关键设计要求）：**
- 所有与后端的交互**只经过一个统一的 API 客户端模块**（`src/api/`）。前端任何地方都不允许直接 `fetch`，必须调用这个模块。
- 后端接口设计成标准的 HTTP + JSON + 轮询/SSE 进度，不依赖浏览器特有能力。
- 满足这两条，将来要套 Tauri 外壳把前端和后端打包成双击即开的 exe，只需替换这一层的传输实现（HTTP → IPC），前端业务代码不用改。

**被否决的选项：**

| 选项 | 否决原因 |
|---|---|
| Tauri 桌面应用 | 用户明确表示"走网页版开发最快，适配性更好"。且需要装 Rust + MSVC（3~7 GB）和首次长编译，与"迭代快"冲突。保留为 P2 可选项 |
| Electron 桌面应用 | 体积 150MB+、内存占用高。用户只有 16GB 内存，跑 AI 任务时 Electron 的额外开销是真实负担 |
| 纯前端无后端（把 ffmpeg 编译成 WASM 在浏览器跑） | ffmpeg.wasm 处理大文件极慢且有内存上限，无法胜任视频放大这类重活 |

### 6.3 技术栈

**决策：全栈 TypeScript，单一运行时。**

```
前端：React 19 + TypeScript + Vite 7 + zustand（状态管理）+ Tailwind CSS 4 + shadcn/ui（组件）
后端：Node.js 22 + TypeScript + Fastify（HTTP 服务）
共享：shared/ 目录存放前后端共用的类型定义与常量
存储：SQLite（结构化数据）+ 本地文件系统（素材二进制）
```

**理由：**
- **只用一个运行时。** 用户已有 Node 22。前后端都是 TypeScript，类型定义可以共享，能消除一整类"前端字段名和后端不一致"的低级 bug。这比"后端用 Python 更顺手"带来的收益大得多。
- **ffmpeg 和 ncnn 二进制只是子进程调用**，用什么语言写后端无关紧要。Node 的 `child_process.spawn` 完全够用。
- **Fastify 相比 Express**：原生 TypeScript 支持更好、内置 JSON Schema 校验、性能更高。相比 NestJS 更轻量，不引入装饰器和依赖注入的复杂度。
- **zustand 相比 Redux**：样板代码少得多。相比 React Context：不会因为状态更新导致整棵树重渲染（这对画布性能很关键）。
- **SQLite 相比 JSON 文件**：需要按项目、按素材、按任务查询和更新，SQLite 的原子事务能避免"程序崩了数据文件损坏"。

**被否决的选项：**

| 选项 | 否决原因 |
|---|---|
| 后端用 Python + FastAPI | 引入第二个运行时和第二个包管理器；用户机器虽已有 Python，但双语言意味着双倍的环境问题和类型不同步风险 |
| 后端用 Rust | 开发慢，用户机器没有 Rust 工具链 |
| 引入 PyTorch 做 AI 放大 | 包体 2~5 GB、启动 10 秒以上；**用户只有 16GB 内存，PyTorch 会和剪辑任务抢内存**。而 Real-ESRGAN 和 RIFE 都有 ncnn-vulkan 的免 Python 预编译二进制，功能等价 |
| 引入 Docker | **用户机器未安装 Docker**，且 Windows 上装 Docker Desktop 还要额外装 WSL2，门槛过高 |
| 用 JSON 文件替代 SQLite | 并发写入和崩溃恢复不可靠；素材和任务会频繁更新 |

### 6.4 第三方依赖的许可证红线

**决策：只允许 MIT / Apache-2.0 / BSD 系列许可证的依赖。以下清单是明确的黑名单。**

这是本 PRD 最容易被忽视但后果最严重的一条——许可证问题是法律问题，不是技术问题。

| 项目 | 许可证 | 结论 |
|---|---|---|
| `tldraw` | 自定义（生产环境需付费 license key，免费档强制水印且禁止商用） | **禁用** |
| `Video2X` | **AGPL-3.0** | **禁用**。AGPL 传染性比 GPL 更强，一旦分发应用会要求开源整个 Canvora。改用 Real-ESRGAN（BSD-3）+ RIFE（MIT）自己串流水线，功能等价 |
| `ComfyUI` | GPL-3.0 | **禁用**（不作为代码依赖；若仅作为用户自行安装的外部服务，另需评估） |
| `ncounterspecialist/twick` | Sustainable Use License（源码可见但非开源，商用受限） | **禁用**。GitHub 显示 `NOASSERTION`，属于危险信号 |
| `openvideodev/react-video-editor` | 自定义 OpenVideo License（个人/小团队免费，超出需付费） | **禁用** |
| `hero8152/Infinite-Canvas` | 自定义（禁止商用、二次开发须开源署名） | **禁用**（连代码参考都要注意——只借鉴思路，不复制代码） |
| `MotzifyStudio/Motz-Whiteboard` | **无 LICENSE 文件** | **禁用**。无许可证 = 默认保留全部权利，法律上不可用 |
| `AIEraDev/Clypra` | MIT | 可用（参考架构） |
| `AmitDigga/fabric-video-editor` | MIT | 可用（参考时间轴实现） |
| `omni-media/omniclip` | MIT | **推荐参考**：clip/effect 数据模型、时间轴交互、WebCodecs 导出流程都是现成 MIT 资产 |
| `schahriar/mfx` | MIT | 可用（编解码层参考） |
| `UnderHear/EaseCut`（npm `easecut-react`） | MIT | 可用（备选时间轴组件，有中文文档，但 star 数少） |
| `elahlabs/elah` | Apache-2.0 | 可用（备选时间轴组件） |
| `OpenTimelineIO` | Apache-2.0 | 可用（时间轴数据模型参考，影视工业标准） |
| `realesrgan-ncnn-vulkan` | BSD-3-Clause | 可用 |
| `rife-ncnn-vulkan` | MIT | 可用 |
| `realcugan-ncnn-vulkan` | MIT | 可用 |
| `ffmpeg`（gyan.dev essentials） | LGPL / GPL（构建相关） | 可用（用户自用） |

**执行要求：** 每新增一个依赖，都必须：
1. 打开它的 `LICENSE` 文件**实际阅读**，不要相信 README 里写的"open source"。
2. 在 `DEPENDENCIES.md` 里登记：包名、版本、许可证全称、用途。
3. 如果 GitHub 侧栏显示 `NOASSERTION` 或 `Other`，**必须人肉读 LICENSE 文件**，读不懂就在交付说明里标出来问用户。

### 6.5 AI 接口的接入策略

**决策：实现一个通用的"服务商配置系统"，内置预置模板，同时允许用户自己填写任意服务商。**

**理由：** 用户明确要求"要有提供我自己选择填写相关的图片、视频、文本等大模型的功能，我后期自己选择加上去就完事了"。所以**配置系统是核心功能，不是附属功能**。

**协议层的现实（这决定了实现复杂度）：**

| 层面 | 现实情况 |
|---|---|
| 图像接口 | **有事实标准**：OpenAI 兼容的 `POST /v1/images/generations`。火山方舟、智谱、阿里兼容模式、OpenAI 都遵守。但细节不统一（`size` 写法、`n`、`response_format`、多图参考字段名）。 |
| 视频接口 | **没有标准**。火山 `/api/v3/contents/generations/tasks`、可灵 `/v1/videos/image2video`（JWT 鉴权）、阿里 `services/aigc/video-generation/video-synthesis`、MiniMax `/v2/video_generation`、智谱 `videos.generations` —— 五套完全不同的路径、参数和状态机。**每家必须单独写适配器。** |
| 异步模式 | 国内视频 API 几乎都是"POST 提交 → 拿 task_id → GET 轮询 → 取 URL"。**但都没有 webhook**，必须自己写轮询调度器。 |
| 结果 URL 有效期 | **结果 URL 常常几小时后就失效**（混元生图 URL 仅 1 小时有效）。**必须拿到结果立刻下载落盘，不能只存 URL。** |

**统一抽象层设计（这是核心工程要求）：**

定义统一的 `GenerationTask` 抽象，包含：`provider`、`kind`（图/视频/文本）、`params`、`remoteTaskId`、`status`、`progress`、`assets[]`、`error`。所有适配器实现同一组接口：

```
submit(provider, params) -> { remoteTaskId }
poll(provider, remoteTaskId) -> { status, progress, resultUrls[] }
```

加一层**通用轮询调度器**负责：指数退避轮询、并发上限控制（各家不同：智谱并发 5~20、混元默认 1）、失败重试、任务持久化（关掉软件再打开还能续上）、拿到结果立刻下载。

**预估适配器数量：** 图像 1 个（OpenAI 兼容，覆盖多数家）+ Gemini 协议 1 个；视频每家 1 个。**收敛目标：先做 3 家。**

**优先接入顺序（按个人可开通性 + 成本 + 覆盖面）：**

| 优先级 | 服务商 | 能力 | 为什么 | 个人能否开通 |
|---|---|---|---|---|
| 1 | **阿里云百炼（通义万相）** | 文生图 / 图生图 / 文生视频 / 图生视频 / **首尾帧** | **一家覆盖全部能力**，人民币结算支付宝充值，异步接口形态统一（都是 `services/aigc/*`），一个适配器吃掉全部 | ✅ 个人实名即可；未实名也能用免费额度（用完即停） |
| 2 | **智谱 bigmodel** | 文生图（cogview-4，**约 0.06 元/张**）/ 文生视频 / 图生视频 / **首尾帧**（cogvideox-3，**1 元/次**） | **价格最低**，试错友好；图像走 OpenAI 兼容端点可复用第一个适配器；视频原生支持首尾帧 | ✅ 个人实名即可 |
| 3 | **火山方舟（Seedream 生图）** | 文生图 / 图生图 | 图像侧 OpenAI 兼容、大陆直连、文档质量最高；中文海报与长文本渲染国内最强 | ✅ 个人实名即可（**但视频见下方警告**） |
| 4（选装） | MiniMax 海螺 | 文生/图生视频、**原生音频**、4~15 秒时长 | 需要原生音频或更长时长时加它 | ✅ 个人实名 + 微信充值 |
| 5 | **DeepSeek** | **仅文本对话** | 用户已有 key。用于提示词节点、AI 对话、提示词扩写 | ✅ 有 key 即可 |

**必须写进界面和文档的现实警告（这些不是可选项）：**

1. **火山引擎 Seedance 视频 API 明确只面向完成企业认证的主体，个人用户不纳入申请范围。** 解锁完整能力还需签保底协议 + 支付预付款 + 百万元级保证金。因此"用 Seedance 生视频"这条路对个人用户**不通**。Canvora 的视频生成走阿里万相 / 智谱 CogVideoX / MiniMax。
2. **可灵 Kling API 需要白名单人工审核**（提交《API接入需求说明书》），且预付费标准包起步约 700 美元。个人工具阶段不划算，列为选装。
3. **DeepSeek 不支持图片生成**，也不支持图片输入（仅实验性 vision 模型支持图片理解）。它只能做文本能力。
4. **Gemini / OpenAI / fal.ai / Replicate 需要代理 + 海外信用卡**，且 Gemini 的限制基于 IP（用海外账号 + 香港节点同样被拒）。这些做成"高级选项"，不作为主路径。
5. **不要自建统一 API 网关**（如 one-api / new-api / LiteLLM）。这些项目官方只提供 Docker 部署路径，用户机器没装 Docker；自己编译维护成本高于直接写 3 个适配器。**但要在设置界面保留"自定义 OpenAI 兼容 Base URL"入口**，让高级用户能指向任何兼容中转站，这样不承担网关维护责任但把门打开。

### 6.6 视频预览与剪辑引擎

**决策：后端 ffmpeg 生成低码率代理文件（proxy）用于预览；导出时用同一套滤镜定义作用在原片；视频元素用 DOM `<video>` 浮在画布上层。**

**理由：**

**为什么用 proxy 而不是直接播原片：**
预览要流畅，就必须用低分辨率低码率的小文件。但更重要的是**保证"预览看到的"和"导出的"一致**。做法是：预览和导出共用同一份滤镜定义（同一个数据结构），proxy 只降低分辨率、不改变处理语义。这样能消掉绝大多数"预览是对的、导出不对"的问题。

**为什么视频用 DOM 而不是画进 canvas：**
- 画进 canvas 意味着每帧走主线程 + React 重绘，同时播几个 1080p 视频就会打满主线程。
- 画进 canvas 会**丢失浏览器对 `<video>` 的独立 GPU 合成层**，这是原生播放器流畅的根源。
- 正确架构：**画布只负责布局与变换，媒体像素交给 DOM 元素。** 用一个祖先容器统一做 `transform: scale/translate`，所有 `<video>` 绝对定位并加 `will-change: transform`，浏览器会走 GPU 合成，缩放平移时不触发重排。

**为什么不用 WebCodecs 在浏览器内解码：**
工程量是几个月级（要自写 demux → 解码队列 → 帧序管理 → 音画同步），而收益只是"比 seeks 更精确"。而且 WebCodecs 的硬件解码可用率受驱动和系统编解码器影响很大（H.265 必须装系统 HEVC 扩展，否则静默失败）。**Canvora 有 ffmpeg，没有理由在浏览器里重新实现解码器。**

**预览与导出不一致的三类典型来源（必须显式处理）：**

| 来源 | 表现 | 处理方式 |
|---|---|---|
| **旋转元数据** | 手机拍的视频有 `rotate=90` 的显示矩阵，`<video>` 会自动应用，ffmpeg 默认不会 → 预览横竖屏和导出不一致 | **导入素材时统一转码归一化**，把旋转烧进像素 |
| **色彩空间** | `yuv420p` vs `yuvj420p`、BT.601 vs BT.709、全范围/有限范围 → 预览和导出偏色 | 归一化时统一 `-pix_fmt yuv420p -colorspace bt709` |
| **时间基** | `<video>.currentTime` 是浮点秒，ffmpeg 用 PTS | 统一以秒为单位、保留 3 位小数，只在生成滤镜图时换算 |

**关键设计要求：素材导入时做一次"归一化转码"**，输出固定分辨率上限、`yuv420p`、BT.709、旋转已烧入的中间格式，之后所有预览和导出都基于它。这一步成本很低，但能消掉约 80% 的玄学问题。

**proxy 规格：** `scale=-2:480`（高度 480，宽度按比例）、`-crf 30`、`-preset veryfast`、`-g 30`（关键帧间隔 1 秒，保证 seek 快速）、`-movflags +faststart`、音频 AAC 64k。

**被否决的选项：**

| 选项 | 否决原因 |
|---|---|
| 前端 `<video>` + CSS 变换做预览，导出时用 ffmpeg 重放滤镜链（无 proxy） | 一致性靠"祈祷"，旋转/色彩/时间基三类偏差必然发生 |
| WebCodecs 解码 → canvas 渲染 | 工程量数月级，收益有限；硬件解码可用率不确定 |
| 把 `<video>` drawImage 进 canvas 播放 | 主线程被打满、丢失 GPU 合成层 |
| 引入 WebGL/PixiJS 做合成预览 | 本期不需要帧精确合成预览；引入渲染引擎的复杂度与收益不匹配 |

### 6.7 数据存储

**决策：**

| 数据 | 存储位置 | 说明 |
|---|---|---|
| 项目、画布、节点、连线、时间轴、片段、任务记录 | **SQLite**（工作区内 `canvora.db`） | 需要事务与查询 |
| 素材二进制（图片/视频原片） | 工作区 `projects/<项目ID>/assets/` | 用户可直接用资源管理器找到 |
| proxy 预览文件 | 工作区 `projects/<项目ID>/proxy/` | 可随时删除重建，删除后自动重新生成 |
| 导出成品 | 工作区 `projects/<项目ID>/exports/` | |
| 临时中间文件 | 工作区 `projects/<项目ID>/temp/` | 任务结束或启动时清理 |
| 本地 AI 二进制与模型 | 工作区 `bin/`、`models/` | 见 5.3 |
| API 密钥 | 工作区 `secrets.json`（加密）+ 环境变量兜底 | 见 6.8 |

**工作区目录结构（固定，不要在实现时随意改动）：**

```
<用户选择的工作区根目录>/           例如 F:/Canvora
├── canvora.db                      SQLite 数据库
├── secrets.json                    加密后的密钥（加入 .gitignore）
├── bin/                            ffmpeg.exe / realesrgan-*.exe / rife-*.exe
├── models/                         AI 模型权重
├── luts/                           调色用的 .cube LUT 文件
├── cache/
│   └── thumbs/                     全局缩略图缓存
└── projects/
    └── <projectId>/
        ├── project.json            项目元信息
        ├── assets/                 原片
        ├── proxy/                  预览代理文件
        ├── exports/                导出成品
        └── temp/                   临时文件
```

**素材文件命名：** 存进 `assets/` 时重命名为 `<assetId>.<ext>`（assetId 为 nanoid），**同时在数据库里保留原始文件名**。这样避免中文/空格路径问题，也不丢用户信息。

### 6.8 密钥安全

**决策：密钥加密存本地文件，界面上明确告知风险，并明确建议只存放有余额上限的 key。**

**必须承认的事实（写进界面提示文案）：**
> 运行在你电脑上的程序，其保存的密钥无法做到真正保密。任何存放在本机文件里的密钥，理论上都能被读取。请只填写设置了消费上限的子密钥，不要使用主账号密钥。

**实现要求：**
- 使用 `secrets.json` 存储，字段值用 Node 的 `crypto` 模块基于本机信息派生密钥加密（提高门槛即可，不追求绝对安全）。
- `secrets.json` 必须加入 `.gitignore`。
- **前端永远不直接持有密钥**：所有调用 AI 接口的请求都从后端发出，前端只发送"用哪个 provider"的标识。
- 界面上密钥输入框使用 password 类型，提供"显示"切换按钮。
- 提供"测试连接"按钮，让用户在保存前验证密钥有效。

**被否决的选项：**
- **为保护密钥而自建云端中转服务**：会把 Canvora 从"本地工具"变成"需要承担用户密钥托管的服务"，成本和合规风险跳一个量级。
- **用系统级凭据管理（Windows Credential Manager / DPAPI）**：作为可选增强可以，但不是必需。本版本用文件加密即可，因为威胁模型是"本机文件被读取"，而能读文件的人通常也能读凭据存储。

---

## 7. 系统架构

### 7.1 分层

```
┌──────────────────────────────────────────────────────────────┐
│  浏览器（用户界面）                                             │
│  ┌────────────────┐  ┌────────────────┐  ┌─────────────────┐ │
│  │  画布层         │  │  时间轴层       │  │  面板层          │ │
│  │  DOM 节点       │  │  Konva/Canvas   │  │  素材库/任务中心  │ │
│  │  + SVG 连线     │  │  片段/播放头     │  │  provider 配置   │ │
│  │  + DOM 视频浮层  │  │                │  │                 │ │
│  └────────────────┘  └────────────────┘  └─────────────────┘ │
│  ┌──────────────────────────────────────────────────────────┐│
│  │  src/api/  ← 唯一的前后端边界，所有请求必须走这里            ││
│  └──────────────────────────────────────────────────────────┘│
└───────────────────────────┬──────────────────────────────────┘
                            │ HTTP + JSON（将来可替换为 Tauri IPC）
┌───────────────────────────┴──────────────────────────────────┐
│  Node.js 后端（127.0.0.1，仅本机监听）                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐ │
│  │ 素材服务  │ │ 画布/项目 │ │ 任务队列  │ │  AI 适配层        │ │
│  │ 静态/范围 │ │ CRUD     │ │ 串行调度  │ │  OpenAI兼容/各家  │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘ │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────────┐ │
│  │ 滤镜图    │ │ proxy    │ │ 本地AI    │ │  密钥管理         │ │
│  │ 编译器    │ │ 生成器    │ │ 编排      │ │                 │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────────┘ │
└───────────────────────────┬──────────────────────────────────┘
                            │ child_process.spawn
┌───────────────────────────┴──────────────────────────────────┐
│  外部进程                                                     │
│  ffmpeg.exe    realesrgan-ncnn-vulkan.exe    rife-ncnn-vulkan.exe │
└──────────────────────────────────────────────────────────────┘
```

### 7.2 进程模型

| 进程 | 数量 | 说明 |
|---|---|---|
| Node.js 后端 | 1 | 主进程，承载 HTTP 服务、数据库、任务队列 |
| 浏览器标签页 | 1+ | 前台界面 |
| ffmpeg 子进程 | 0~1 | 同一时刻只跑一个（避免 16GB 内存被吃满） |
| ncnn 子进程（放大/补帧） | 0~1 | 同一时刻只跑一个（显存独占） |

**关键约束：所有重活串行。** 同时跑两个 ffmpeg 或"ffmpeg + ncnn"会导致内存/显存争抢卡死。任务队列必须是全局串行队列，界面上显示"排队中（前面还有 N 个任务）"。

### 7.3 数据流：一次 AI 生成（以首尾帧生视频为例）

```
1. 用户在画布上点"生视频"节点的运行按钮
2. 前端执行引擎做拓扑排序，收集该节点的全部上游节点
3. 前端从上游节点抽取输入：
   - 提示词节点 → 文本
   - 两张图片节点 → 首帧图、尾帧图（本地文件路径）
4. 前端调用 src/api/ 提交任务，带 providerId、modelId、参数、图片路径
5. 后端：
   a. 读取密钥，解密
   b. 若接口需要公网可访问的图片 URL → 先上传到图床或转 base64（见 9.4 的说明）
   c. 调用对应适配器的 submit()，拿到 remoteTaskId
   d. 写入 SQLite 的任务表，状态 queued
   e. 返回 taskId 给前端
6. 前端订阅该任务的进度（轮询后端 /api/tasks/:id，或 SSE 推送）
7. 后端轮询调度器：
   a. 按指数退避调用适配器的 poll()
   b. 更新数据库状态与进度，前端界面实时刷新
   c. 状态变为 succeeded 后，**立刻下载结果文件**到 assets/，生成 assetId
   d. 若为视频，同时生成缩略图与 proxy
   e. 更新数据库，关联 assetId
8. 前端收到完成通知，在画布上该节点下方自动创建一个视频节点，指向新素材
9. 任务记录在"任务中心"可查，失败的任务可一键重试
```

### 7.4 数据流：一次剪辑导出

```
1. 用户在时间轴上编辑序列（分割、变速、贴图、调色）
2. 前端把编辑状态序列化为一个 "EditDecisionList" 结构（见 8.7）
3. 用户点导出 → 前端调用 src/api/ 提交导出任务
4. 后端滤镜图编译器：
   a. 校验 EDL（片段是否连续、变速范围是否合法、贴图时间窗是否在片段内）
   b. 为每个片段生成独立的视频/音频处理后缀（链）
   c. 用 concat 滤镜把多段串起来
   d. 把贴图 overlay 与调色滤镜接在拼接之后
   e. 输出完整的 filter_complex 文本
5. 后端把 filter_complex 写入工作区 temp/ 下的临时 .txt 文件
6. 后端 spawn ffmpeg，参数以数组形式传递（不拼 shell 字符串）：
   ["-y", "-i", ..., "-filter_complex_script", tmpFile, "-map", "[v]", ...]
7. 解析 ffmpeg 的 stderr 输出进度（ffmpeg 通过 -progress pipe:1 输出结构化进度）
8. 完成后写入 exports/，在任务中心显示"导出完成"，提供"打开所在文件夹"按钮
9. 删除临时文件
```

---

## 8. 数据模型

以下为 TypeScript 接口定义，前后端共用（放在 `shared/types/`）。字段名一律使用英文，界面文案用中文。

### 8.1 项目 Project

```ts
interface Project {
  id: string;              // nanoid
  name: string;            // 用户可改，默认"未命名项目"
  createdAt: number;       // Unix 毫秒
  updatedAt: number;
  thumbnailAssetId?: string;  // 封面，取画布上第一个素材
  // 画布视口状态（用于下次打开时恢复视角）
  viewport: { x: number; y: number; zoom: number };
}
```

### 8.2 节点 Node

```ts
type NodeKind =
  | 'prompt'        // 提示词（纯文本，可手动编辑）
  | 'text'          // 普通文本 / 备注
  | 'image'         // 图片素材引用
  | 'video'         // 视频素材引用
  | 'audio'         // 音频素材引用
  | 'generateImage' // 生图配置节点
  | 'generateVideo' // 生视频配置节点
  | 'llm'           // 文本模型调用节点（如 DeepSeek 扩写提示词）
  | 'upscale'       // 本地放大配置节点
  | 'interpolate'   // 本地补帧配置节点
  | 'extractFrame'  // 抽帧节点（首帧/尾帧/指定时间）
  | 'group'         // 分组框（纯视觉容器）
  | 'note'          // 便利贴

interface Node {
  id: string;
  projectId: string;
  kind: NodeKind;
  x: number;               // 画布坐标（不是屏幕坐标）
  y: number;
  width: number;
  height: number;
  zIndex: number;
  title: string;           // 节点标题，用户可改
  data: NodeData;          // 按 kind 区分，见下
  runState?: RunState;     // 仅生成类节点有
  createdAt: number;
  updatedAt: number;
}

type RunState = {
  status: 'idle' | 'queued' | 'running' | 'succeeded' | 'failed' | 'cancelled';
  progress?: number;       // 0~1
  taskId?: string;         // 关联 GenerationTask
  error?: string;          // 人类可读的中文错误说明
  lastRunAt?: number;
};
```

**各 kind 的 data 结构：**

```ts
// prompt / text / note
{ text: string }

// image / video / audio：引用一个素材
{ assetId: string; posterPath?: string; durationSec?: number }

// generateImage
{
  providerId: string | null;      // null = 未配置
  modelId: string | null;
  promptOverride?: string;        // 若为空则用上游提示词节点的文本
  negativePrompt?: string;
  count: number;                  // 一次生成几张，默认 1
  size: string;                   // 如 "1024x1024"
  seed?: number | null;
  extra: Record<string, unknown>; // 透传给接口的其他参数
}

// generateVideo
{
  providerId: string | null;
  modelId: string | null;
  promptOverride?: string;
  mode: 'text2video' | 'image2video' | 'frames';  // frames = 首尾帧
  durationSec: number;            // 如 5
  resolution: '480p' | '720p' | '1080p';
  aspectRatio: '16:9' | '9:16' | '1:1';
  fps?: number;
  seed?: number | null;
  extra: Record<string, unknown>;
}

// llm
{
  providerId: string | null;
  modelId: string | null;
  systemPrompt?: string;
  userPromptTemplate: string;   // 支持 {{input}} 占位符插入上游文本
  temperature: number;
}

// upscale
{
  engine: 'realesrgan' | 'realcugan';
  scale: 2 | 3 | 4;
  model: string;               // 如 'realesrgan-x4plus' | 'realesr-animevideov3'
  tileSize: number;            // 默认 128，低显存时调小
  targetKind: 'image' | 'video';
}

// interpolate
{
  multiplier: 2 | 4;           // 2x / 4x 帧率
  model: string;               // 如 'rife-v4.6'
  targetFps?: number;
}

// extractFrame
{ position: 'first' | 'last' | number }  // number = 秒

// group
{ color: string; collapsed: boolean }
```

### 8.3 连线 Edge

**关键设计：连线必须携带语义（传的是提示词、图片还是视频），不能靠运行时猜。**

```ts
type PortKind = 'text' | 'image' | 'video' | 'audio' | 'any';

interface Edge {
  id: string;
  projectId: string;
  fromNodeId: string;
  fromPortId: string;        // 输出口标识，如 'out'
  toNodeId: string;
  toPortId: string;          // 输入口标识，如 'prompt' / 'firstFrame' / 'lastFrame' / 'refImage'
  kind: PortKind;            // 这条线上流的是什么
  createdAt: number;
}
```

**端口定义（每个节点类型声明自己的输入/输出口）：**

| 节点类型 | 输入口 | 输出口 |
|---|---|---|
| prompt / text / note | 无 | `out: text` |
| image | 无 | `out: image` |
| video | 无 | `out: video` |
| audio | 无 | `out: audio` |
| generateImage | `prompt: text`、`refImage: image`（可多个） | `out: image`（可多个） |
| generateVideo | `prompt: text`、`firstFrame: image`、`lastFrame: image`、`refImage: image`（可多个） | `out: video`（可多个） |
| llm | `input: text` | `out: text` |
| upscale | `in: image \| video` | `out: image \| video` |
| interpolate | `in: video` | `out: video` |
| extractFrame | `in: video` | `out: image` |

**连线规则：**
- 只有 `PortKind` 兼容才能连（`any` 与所有类型兼容；`text` 不能连到 `image` 输入口）。
- 一个输入口只接受一条连线（除非标注"可多个"）。
- 禁止成环：连接前检查是否会在图中形成环，会则拒绝并在界面上提示"不能连成循环"。

### 8.4 素材 Asset

```ts
type AssetKind = 'image' | 'video' | 'audio';

interface Asset {
  id: string;
  projectId: string | null;    // null 表示在全局素材库里（不属于任何项目）
  kind: AssetKind;
  originalName: string;        // 用户原始文件名，用于显示
  relPath: string;             // 相对工作区的路径，如 projects/xxx/assets/abc123.mp4
  ext: string;
  sizeBytes: number;
  width?: number;
  height?: number;
  durationSec?: number;        // 视频/音频
  fps?: number;
  codec?: string;
  hasAudio?: boolean;
  thumbRelPath?: string;       // 缩略图
  proxyRelPath?: string;       // proxy 文件
  proxyStatus: 'none' | 'pending' | 'ready' | 'failed';
  source: 'imported' | 'generated' | 'upscaled' | 'interpolated' | 'extracted';
  sourceTaskId?: string;       // 若为生成产物，关联任务
  tags: string[];              // 用户自定义标签
  favorite: boolean;
  createdAt: number;
}
```

### 8.5 生成任务 GenerationTask

```ts
type TaskKind = 'image' | 'video' | 'text' | 'upscale' | 'interpolate' | 'extractFrame' | 'export' | 'proxy';

interface GenerationTask {
  id: string;
  projectId: string;
  nodeId?: string;             // 由哪个节点发起
  kind: TaskKind;
  engine: 'cloud' | 'local';   // 云端接口 或 本地二进制
  providerId?: string;         // 云端任务才有
  modelId?: string;
  params: Record<string, unknown>;
  remoteTaskId?: string;       // 云端返回的任务 ID
  status: 'queued' | 'running' | 'succeeded' | 'failed' | 'cancelled';
  progress: number;            // 0~1
  statusText: string;          // 中文状态描述，直接显示给用户
  resultAssetIds: string[];
  errorMessage?: string;       // 中文，面向用户
  errorDetail?: string;        // 技术细节，折叠显示
  retryCount: number;
  createdAt: number;
  startedAt?: number;
  finishedAt?: number;
}
```

**要求：任务必须持久化到 SQLite。** 应用重启后，状态为 `running` 的云端任务应恢复轮询（因为云端任务仍在跑）；状态为 `running` 的本地任务应标记为失败（因为子进程已经死了），提示用户重新执行。

### 8.6 服务商配置 Provider

```ts
type ProtocolType = 'openai-compatible' | 'openai-images' | 'ark-video' | 'dashscope-video' | 'zhipu-video' | 'minimax-video' | 'gemini' | 'custom-script';

type ModelCapability = 'text' | 'text2image' | 'image2image' | 'text2video' | 'image2video' | 'firstLastFrame' | 'imageEdit';

interface Provider {
  id: string;
  name: string;                 // 用户自定义显示名，如"我的通义万相"
  protocol: ProtocolType;
  baseUrl: string;
  apiKeyRef: string;            // 指向 secrets.json 的键名，不存明文
  enabled: boolean;
  models: ProviderModel[];
  isPreset: boolean;            // 是否为内置预置（预置也可编辑副本）
  createdAt: number;
}

interface ProviderModel {
  id: string;                   // 模型 ID，如 'wan2.2-kf2v-flash'
  displayName: string;          // 界面显示名
  capabilities: ModelCapability[];
  // 该模型支持的参数范围，用于界面生成合适的控件
  paramHints?: {
    durations?: number[];
    resolutions?: string[];
    aspectRatios?: string[];
    maxRefImages?: number;
    maxCount?: number;
  };
}
```

### 8.7 时间轴 Timeline / 轨道 Track / 片段 Clip

```ts
interface Timeline {
  projectId: string;
  durationSec: number;          // 序列总时长（由片段推算，不手工设置）
  fps: number;                  // 导出帧率，默认 30
  width: number;                // 导出分辨率，默认取第一个片段
  height: number;
  backgroundColor: string;      // 默认 '#000000'
  tracks: Track[];
}

interface Track {
  id: string;
  kind: 'video' | 'overlay' | 'audio';
  index: number;                // 同类型内的顺序，从 0 开始
  muted: boolean;
  locked: boolean;
  clips: Clip[];
}

interface Clip {
  id: string;
  trackId: string;
  assetId: string;              // 引用的素材

  // 源时间：引用素材的哪一段（单位秒）
  inPoint: number;
  outPoint: number;             // 不含

  // 时间轴位置：在序列上的起始时间（单位秒）
  startAt: number;

  // 变速：1.0 = 原速；2.0 = 两倍速；0.1 = 十分之一速度（慢放）
  speed: number;

  // 由上面三个值推算，不单独存储（避免数据不一致）
  // sourceDurationSec = outPoint - inPoint
  // timelineDurationSec = sourceDurationSec / speed

  // 变速时的音频处理
  audioMode: 'keep' | 'mute' | 'keepPitchCorrected';

  // 调色（作用于这个片段）
  colorGrade?: ColorGrade;

  // 这个片段上的贴图层
  overlays?: Overlay[];

  // 转场（本期只支持硬切，保留字段但不实现）
  transitionIn?: null;

  enabled: boolean;
}

interface ColorGrade {
  // 全部为相对值，不填表示不改动
  brightness?: number;      // -1.0 ~ 1.0，默认 0
  contrast?: number;        // 0.0 ~ 3.0，默认 1
  saturation?: number;      // 0.0 ~ 3.0，默认 1
  gamma?: number;           // 0.1 ~ 3.0，默认 1
  temperature?: number;     // 色温偏移，-1.0 ~ 1.0，正值偏暖
  tint?: number;            // 色调偏移，-1.0 ~ 1.0，正值偏绿
  lutPath?: string;         // 可选，.cube 文件的相对路径
}

interface Overlay {
  id: string;
  assetId: string;          // 贴图素材（图片）
  // 位置：相对于视频画面的归一化坐标（0~1），便于不同分辨率复用
  x: number;                // 左上角 x，0 = 最左
  y: number;
  widthRatio: number;       // 宽度占比，0~1
  // 时间窗：在片段内生效的时间范围（相对片段起点，单位秒）
  startSec: number;
  endSec: number;
  opacity: number;          // 0~1
  // 淡入淡出时长（秒），0 表示不做
  fadeInSec: number;
  fadeOutSec: number;
}
```

**「EditDecisionList」（EDL）** 是提交给滤镜图编译器的完整结构：即 `Timeline` + 所有片段引用的素材的**绝对路径解析结果**。前端提交给后端的不是 `Timeline` 本身（因为包含 assetId），而是解析后的 EDL（包含真实文件路径）。

### 8.8 应用设置 AppSettings

```ts
interface AppSettings {
  workspaceRoot: string;              // 必须由用户选择，不允许默认 C 盘
  ffmpegPath: string;                 // 默认 'ffmpeg'（走 PATH）
  ffprobePath: string;
  realesrganPath: string;             // 工作区内 bin/ 的绝对路径
  rifePath: string;
  defaultProviderId: Record<ModelCapability, string | null>;  // 各能力默认用哪家
  concurrency: {
    localTasks: 1;                    // 固定为 1，不可改（见 5.2）
    cloudTasks: number;               // 默认 3，可调 1~6
  };
  theme: 'light' | 'dark' | 'system';
  language: 'zh-CN';                  // 固定
}
```

---

## 9. 功能需求

格式：每个模块列出功能点，每个功能点包含 **用户故事 / 交互细节 / 异常与边界 / 验收标准**。

### 模块 A：项目与素材管理

#### A1 工作区初始化

- **用户故事**：作为用户，我第一次打开 Canvora，希望它引导我选一个放数据的文件夹，并且默认建议一个磁盘空间大的位置，而不是把东西塞到快满的 C 盘。
- **交互细节**：
  - 首次启动检测工作区配置是否存在。不存在则弹出一个**不可跳过的**引导页。
  - 引导页列出所有磁盘及其剩余空间（读取磁盘信息），**把剩余空间最大的盘排在第一位并默认选中**，C 盘如果剩余低于 30 GB 则标注红色"不推荐"。
  - 默认建议的目录名：`<盘符>:/Canvora`。
  - 用户确认后创建目录结构（见 6.7），写入设置。
  - 引导页第二步：检测 ffmpeg 是否存在（执行 `ffmpeg -version`），不存在则给出安装指引；检测 `bin/` 下的本地 AI 二进制，不存在则提供"一键下载"按钮（运行 `scripts/fetch-tools.mjs`）并显示下载进度。
  - 引导页第三步：提示去"模型与接口"页面配置 AI 服务商（可跳过，但主界面要有一个常驻的提醒条）。
- **异常与边界**：
  - 用户选的目录不可写 → 明确中文提示"这个文件夹没有写入权限，请换一个"。
  - 用户选的目录已存在且有 `canvora.db` → 询问"发现已有 Canvora 数据，是要打开它还是新建一个？"。
  - 磁盘剩余不足 20 GB → 黄色警告，允许继续但每次启动都提醒。
  - ffmpeg 检测失败 → 不阻塞进入应用，但在导出/放大相关功能上给出明确提示。
- **验收标准**：全新环境启动后，能在 3 步内进入主界面；`F:/Canvora/` 下出现 6.7 节规定的完整目录结构。

#### A2 素材导入

- **用户故事**：作为用户，我想把桌面上的视频和图片拖进 Canvora，或者用文件选择器批量导入。
- **交互细节**：
  - 支持方式：**拖拽文件到画布**（最常用）、拖拽文件夹、顶部"导入"按钮打开文件选择器（支持多选）。
  - 支持的格式：图片 `png/jpg/jpeg/webp/gif/bmp`；视频 `mp4/mov/mkv/webm/avi`；音频 `mp3/wav/aac/m4a`。
  - 导入时用 ffprobe 读取元信息（宽高、时长、帧率、编码、是否有音轨）。
  - 素材复制到 `projects/<id>/assets/<assetId>.<ext>`（**不要只存路径引用**，避免用户移动原文件后素材失效）。
  - 大文件复制时显示进度条，可取消。
  - **自动执行归一化转码**（见 6.6）：对于有旋转元数据、或色彩空间异常、或编码不被浏览器支持的视频，后台生成一个归一化副本。归一化是**后台任务**，不阻塞导入。归一化完成后原片保留（用户可以随时查看原始文件），编辑与预览使用归一化版本。
  - 导入的视频自动排队生成 proxy 和缩略图。
  - 导入完成后，素材自动出现在画布上（导入位置 = 拖拽落点或视口中心；多个文件以网格排列，不重叠）。
- **异常与边界**：
  - 不支持的格式 → 逐个文件提示"`.xyz` 格式不支持"，其他文件继续导入。
  - 文件正在被其他程序占用 → 提示"文件被占用，请关闭正在使用它的程序后重试"。
  - 磁盘空间不足 → 导入前检查，不足则整体拒绝并提示还差多少 GB。
  - 超大文件（>4 GB）→ 允许导入但给出提示"这个文件较大，proxy 生成会比较慢"。
  - 重复导入同一个文件 → 按文件内容哈希判断，提示"这个素材已经在项目里了，是否仍要导入一份？"。
- **验收标准**：拖拽 5 个混合格式文件到画布，全部正确导入并按网格排布；刷新页面后素材仍在；`assets/` 下文件存在且能直接双击播放。

#### A3 素材库

- **用户故事**：作为用户，我想浏览我用过的所有素材，按类型筛选，收藏好用的，打标签，并且直接拖到画布上。
- **交互细节**：
  - 右侧可折叠面板，"当前项目"和"全部素材"两个页签。
  - 网格视图，显示缩略图；视频缩略图上显示时长角标。
  - 筛选：类型（图/视频/音频）、来源（导入/生成/放大/补帧）、收藏、标签。
  - 排序：添加时间（默认倒序）、文件大小、时长。
  - 单机选中，双击/拖拽到画布创建节点。
  - 右键菜单：重命名、加标签、收藏、在资源管理器中显示、删除（删除时提示"引用它的节点会变成空白"）。
  - 搜索框：按原始文件名模糊搜索。
- **异常与边界**：
  - 删除被时间轴片段引用的素材 → 必须先提示"这个素材正在被时间轴使用，删除后会怎样"，让用户确认；确认后片段标记为失效而不是崩溃。
  - 素材文件在磁盘上被用户手动删了 → 界面上显示为"文件丢失"占位图，提供"重新定位"和"移除"两个操作。
- **验收标准**：素材库能正确显示缩略图与时长；筛选、排序、搜索、收藏、标签全部生效；拖到画布能创建节点。

#### A4 多项目管理

- **用户故事**：作为用户，我想为不同的创作任务建不同的项目，随时切换。
- **交互细节**：左上角项目名下拉，支持新建、重命名、复制、删除、切换。切换时保存当前画布视口状态。
- **异常与边界**：删除项目时提示"项目里的素材文件也会一起删除，共 N 个文件、约 X GB"，需要用户二次确认（输入项目名确认）。
- **验收标准**：能建 3 个以上项目并自由切换，各项目画布内容互不干扰。

### 模块 B：无限画布

#### B1 画布视口

- **用户故事**：作为用户，我想用鼠标滚轮缩放、按住空格或中键拖拽平移，并且能一键回到全部内容。
- **交互细节**：
  - 滚轮缩放：**以鼠标指针位置为锚点**（指针下的内容位置保持不动）。缩放范围 0.1x ~ 4x。
  - 平移：按住中键拖拽、或按住空格 + 左键拖拽、或双指触控板滑动。
  - 框选：在空白处按下左键拖拽出选框，选中框内节点。
  - 快捷键：`Ctrl+0` 缩放到 100%、`Shift+1` 缩放到适应全部内容、`Ctrl+=` / `Ctrl+-` 缩放。
  - 右下角显示当前缩放比例，点击可输入精确值。
  - 右下角小地图（可折叠），显示所有节点位置与当前视口矩形，点击可跳转。
  - 背景网格点阵，随缩放变化密度（缩放很小时不画网格，省性能）。
- **实现要求（性能关键）**：
  - **所有节点放在一个祖先容器里，该容器只用一个 CSS `transform: translate(x,y) scale(z)`。** 不要给每个节点单独设置位置变换后随缩放重算。这样浏览器走 GPU 合成，缩放平移时不触发重排。
  - 平移/缩放期间给容器加 `will-change: transform`，操作停止 200ms 后移除（避免长期占用显存）。
  - 节点用 `React.memo`，且**节点组件不允许订阅整个节点列表**（必须通过 id 精确订阅自己的数据），否则拖动一个节点会导致全部节点重渲染。
  - 使用视口裁剪：视口外的节点不渲染（或只渲染一个占位矩形）。
- **验收标准**：在 100 个节点的画布上，缩放平移保持流畅（用浏览器开发者工具的 FPS 面板观察，无持续掉帧）；指针锚点缩放准确（指针下的节点位置不漂移）。

#### B2 节点渲染与交互

- **用户故事**：作为用户，我想拖动节点、调整大小、复制粘贴、删除、对齐，并且节点上的控件能直接用。
- **交互细节**：
  - 拖动：按住节点标题栏或空白区域拖动。多选时拖动会一起移动。
  - 调整大小：选中后出现八个控制点，拖拽改变尺寸。图片/视频节点按 Shift 保持宽高比。
  - 选中：单击选中，Shift 加选，在空白处拖拽框选，`Ctrl+A` 全选。
  - 复制粘贴：`Ctrl+C` / `Ctrl+V`（粘贴时偏移 20px，避免重叠），`Ctrl+D` 直接复制。
  - 删除：`Delete` 键。
  - 对齐辅助线：拖动时如果与其他节点的边缘或中线接近（8px 内），显示辅助线并吸附。
  - 层级：右键菜单"置顶/置底/上移一层/下移一层"。
  - 分组：框选多个节点后 `Ctrl+G` 成组，组可以折叠。
  - **视频节点**：默认显示缩略图 + 播放按钮（不自动播放）；点击播放后在原地播放，显示自制的进度条与时间码；鼠标移开或播放结束回到缩略图态。**同一时刻只允许一个视频节点处于播放状态**（播放新的自动暂停上一个），这是 16GB 内存机器上的必要约束。
  - **视频节点右键菜单**：抽取首帧 / 抽取尾帧 / 抽取当前帧 → 在下方创建一个图片节点。
- **异常与边界**：
  - 节点拖出画布范围外 → 允许，但"适应全部内容"要能把它们找回来。
  - 视频节点引用的素材文件丢失 → 显示占位与"文件丢失"提示。
  - 节点数量很多 → 见 B1 的视口裁剪要求。
- **验收标准**：所有交互生效；拖动 20 个节点同时移动时界面不卡；视频节点右键抽帧能在 1 秒内生成图片节点。

#### B3 连线

- **用户故事**：作为用户，我想把上游节点的输出口拖到下游节点的输入口，建立数据流。
- **交互细节**：
  - 鼠标悬停在节点上时，显示输出口（右侧小圆点）与输入口（左侧小圆点）。
  - 从输出口按下拖拽，出现跟随鼠标的贝塞尔曲线；松开时如果落在合法输入口上则建立连线，否则取消。
  - **拖拽过程中实时高亮所有可连接的合法输入口，非法输入口变灰。** 这是防止用户连错的关键交互。
  - 连线颜色按 `kind` 区分：text 灰、image 蓝、video 紫、audio 绿。
  - 连线中段有一个删除按钮（悬停时显示），点击删除。
  - 选中连线后按 `Delete` 删除。
  - 一个输入口已有连线时，新连线会**替换**旧连线（而不是叠加），并给出提示。
- **异常与边界**：
  - 尝试连接会形成环 → 拒绝并提示"不能连成循环"。
  - 尝试连接类型不兼容的端口 → 拖拽过程中该端口就是灰的，松手无效。
  - 删除节点 → 与其相连的所有连线一并删除。
- **验收标准**：能正确建立/删除连线；非法连接在拖拽阶段就能被识别；环检测生效（构造一个环会被拒绝）。

#### B4 节点执行引擎

这是本模块最核心的部分。

- **用户故事**：作为用户，我在一个生成节点上点"运行"，希望它自动先跑完所有上游需要跑的东西，然后跑自己。
- **交互细节**：
  - 每个生成类节点（`generateImage` / `generateVideo` / `llm` / `upscale` / `interpolate` / `extractFrame`）都有"运行"按钮和"运行到此为止"按钮。
  - 点击运行时：
    1. 收集该节点及其**全部上游节点**的子图。
    2. 做**拓扑排序**（DFS 后序），得到执行顺序。
    3. **环检测**：发现环则拒绝执行并指出涉及哪些节点。
    4. 对顺序中每个"需要执行的节点"（生成类），检查它是否为"脏"（上游有变化或自身参数有变化）。**非脏的节点跳过，直接复用已有结果。**
    5. 依次执行脏节点。上游节点执行完成并产出素材后，自动把素材 ID 填入下游节点的输入。
    6. 每个节点执行时，节点上显示进度环与中文状态文字。
    7. 任意节点失败 → 停止该分支的后续执行，节点变红显示中文错误，提供"重试"（只重跑这个节点及下游）。
  - **取消**：运行中的节点显示"取消"按钮。取消要真正终止后端任务（云端任务调取消接口；本地任务杀子进程树）。
  - **并发限流规则**：
    - 本地任务（放大/补帧）：**全局串行，同时 1 个**。
    - 云端任务：可并发，默认上限 3，用户可在设置里调 1~6。
    - 混合时：本地任务不与任何任务并发。
  - **失败重试**：提供"重试"和"从失败处重跑"两个选项。默认重试 2 次，间隔递增。
  - 运行历史：节点上可展开查看该节点最近的运行记录（时间、耗时、消耗、结果）。
- **异常与边界**：
  - 上游节点没有配置 provider → 明确提示"提示词节点还没连到任何生图模型，请先在设置里配置服务商"。
  - 上游节点产出为空 → 停止并提示。
  - 网络超时 → 按重试策略重试，最终失败给出"网络连接失败，请检查网络后重试"。
  - 云端返回余额不足 → 明确提示"接口返回余额不足，请到服务商后台充值"。
  - 云端返回内容审核不通过 → 明确提示"生成被服务商的内容审核拦截"，并保留原始返回信息供排查。
  - 用户在执行中关闭页面 → **后端继续执行**，重新打开页面时从数据库恢复任务状态并继续显示进度。
- **验收标准**：构造"提示词 → 生图 → 放大"三段链，点最后一节点运行，能自动依次跑完三步并产出最终放大图；中间某步失败时，已成功的步骤结果保留，重试只重跑失败部分。

#### B5 画布导入导出

- **用户故事**：作为用户，我想把整个画布保存成一个文件备份，也想能导入别人的画布。
- **交互细节**：
  - 导出：生成一个 `.canvora` 文件（实际是 zip），包含 `canvas.json`（节点与连线）与**可选**的素材文件。提供"包含素材"和"仅结构"两个选项。
  - 导入：解析并选择"新建项目导入"或"导入到当前画布（追加）"。
  - 版本兼容：文件头写版本号；读到高版本文件时提示"这个文件由更新版本的 Canvora 创建，可能无法完整导入"。
- **异常与边界**：zip 损坏 → 提示"文件已损坏或不是 Canvora 画布文件"，不要崩溃。素材缺失 → 结构导入成功，缺失素材显示为占位。
- **验收标准**：导出的文件在另一台机器/重装后能完整还原画布；仅结构导出时文件小于 1 MB。

### 模块 C：模型与接口配置

#### C1 服务商管理

- **用户故事**：作为用户，我想要一个地方管理我所有的 AI 接口，能自己新增任意服务商，填写地址和密钥，把模型加进去。
- **交互细节**：
  - 设置页"模型与接口"。
  - 列表显示已配置的服务商：名称、协议类型、模型数量、启用状态、连接测试结果。
  - "新增服务商"表单：
    1. **协议类型**下拉（这是最关键的一项）：`OpenAI 兼容（文本/图像）` / `OpenAI 图像生成` / `火山方舟 视频任务` / `阿里百炼 视频任务` / `智谱 视频任务` / `MiniMax 视频任务` / `Gemini` / `自定义脚本`。
    2. 名称（自动填默认名，可改）。
    3. Base URL。
    4. API Key（password 输入框 + 显示切换）。
    5. 鉴权方式（按协议类型自动带出，多数是 Bearer；可灵是 AK/SK 签 JWT；腾讯是 TC3 签名 —— **若某家鉴权复杂，先不实现，在界面上标注"暂不支持"**）。
    6. 模型列表：可手动添加，也可"从接口拉取模型列表"（若该协议支持 `/v1/models`）。
    7. 每个模型勾选**能力标签**（文生图/图生图/文生视频/图生视频/首尾帧/图片编辑/文本对话）。
  - **预置模板**：提供上面提到的几家的一键添加（填好 baseUrl 与默认模型 ID，用户只需填 Key）。
  - **测试连接**按钮：用最小请求验证 Key 与地址有效，返回成功或明确的中文错误（如"密钥无效"、"地址无法访问"、"余额不足"）。
  - **高级：自定义脚本**协议类型，允许用户粘贴一段自己写的请求构造逻辑（用于接入预置都覆盖不了的服务）。**注意：这需要执行用户提供的代码，必须做成沙箱受限模式，或者在界面上明确警告"只填你自己写的或其他你信任的脚本"。**
- **异常与边界**：
  - Key 格式明显不对（如长度过短）→ 前端即时校验提示。
  - 测试连接超时 → 提示"连接超时，可能是地址填错或网络不通"。
  - 同一服务商重复添加 → 允许（用户可能有多套 Key）。
- **验收标准**：能用阿里云百炼的 Key 成功测试连接；能添加一个完全自定义的 OpenAI 兼容服务商并成功调用。

#### C2 能力与模型映射

- **用户故事**：作为用户，我希望在每个生成节点上只看到我能用的模型，并且是按能力筛过的。
- **交互细节**：
  - 生成节点的模型下拉框只列出**具备对应能力**的模型，并按"是否是默认服务商"排序。
  - 设置页有"默认模型"配置：文生图默认用谁、图生视频默认用谁等。
  - 某能力没有任何可用模型时，下拉框显示"暂无可用模型，点这里去配置"。
- **验收标准**：在"生视频"节点上看不到纯文本模型；没有任何生视频模型时给出引导而不是空白下拉。

### 模块 D：AI 生成节点

#### D1 文本节点（提示词 / 文本 / 便利贴）

- **用户故事**：作为用户，我想在画布上写提示词，并且能把它接到生成节点上。
- **交互细节**：
  - `prompt` 节点：多行文本框，支持 `{{变量}}` 语法（预留，本期不做变量替换）。
  - 顶部有一个"AI 扩写"按钮，调用 `llm` 能力把简短提示词扩写成详细的（用配置的文本模型，如 DeepSeek）。
  - `note` 节点：便利贴样式，只有颜色和文本，不能连线。
  - 文本节点可调整大小，文本超出时内部滚动。
- **验收标准**：写一段提示词→接到生图节点→运行，提示词正确传给接口；"AI 扩写"按钮能返回扩写后的文本。

#### D2 生图节点

- **用户故事**：作为用户，我想选模型、填参数、点运行出图，图片直接出现在画布上。
- **交互细节**：
  - 参数控件：模型选择、数量（1~4）、尺寸（按模型能力给出可选值）、种子（可留空随机）、负面提示词（若模型支持）、其他参数（键值对形式，透传）。
  - 运行后进度显示在节点上（云端生图多为同步接口，显示"生成中…"即可）。
  - 出图后：**每张结果自动创建一个图片节点**，排布在该生图节点下方，并与生图节点建立连线（表示来源）。同时自动存入素材库（`source: 'generated'`）。
  - 节点上保留最近一次的结果缩略图。
- **异常与边界**：内容审核拦截 → 明确中文提示，不重试（重试也没用）。返回 base64 而非 URL → 要正确处理两种返回格式。一次生成多张只成功部分 → 成功的建节点，失败的在节点头部标注"3 张中 1 张失败"。
- **验收标准**：用智谱 cogview-4 生成一张图，图片出现在画布上且能被放大节点继续处理。

#### D3 生视频节点（含首尾帧）

- **用户故事**：作为用户，我想用文字或首尾帧生成视频，生成过程中能看到进度，生成完能直接在画布上播放。
- **交互细节**：
  - 模式选择：`文生视频` / `图生视频` / `首尾帧`。**模式决定输入口**：选首尾帧时显示两个输入口（首帧、尾帧）。
  - 参数：模型、时长（按模型能力，如 5s/10s）、分辨率（480p/720p/1080p）、宽高比（16:9/9:16/1:1）、帧率、种子。
  - **异步任务进度**：显示"排队中 → 生成中（第 N 秒） → 下载中 → 完成"的中文状态，以及一个进度条。由于多数接口不返回百分比，进度条用"已等待时间/预计时间"的估算值，并明确标注"预计"。
  - 生成完成后：**立刻下载结果到本地**（不能只存 URL，URL 会过期），创建视频节点与素材记录，自动排队生成缩略图与 proxy。
  - **首尾帧的图片传递问题**：多数接口要求图片是公网可访问的 URL 或 base64。实现优先级：
    1. 优先用 base64（若接口支持），**不引入任何图床依赖**。
    2. 若接口只接受 URL，**提示用户"这个模型要求图片有公网地址，你可以配置一个图床，或改用支持 base64 的模型"**，并在设置里提供图床配置项（可留空）。
    - **绝对不要私自把用户的图片上传到任何公共图床。**
- **异常与边界**：
  - 生成耗时很长（数分钟）→ 页面关闭后任务继续，重新打开能看到进度。
  - 任务在云端失败 → 显示服务商返回的原始错误信息（折叠）+ 中文解释。
  - 任务长时间无响应（超过 30 分钟）→ 标记为超时失败，提供"重新查询一次"按钮（有时云端其实成功了）。
  - 首尾帧两张图尺寸不一致 → 提示用户，并询问是否自动统一到第一张的尺寸。
- **验收标准**：用阿里百炼的首尾帧模型，给两张图，生成出视频并能在画布上播放；关掉页面再打开，任务状态正确恢复。

#### D4 抽帧节点

- **用户故事**：作为用户，我想从一个视频里抽出一帧当图片用（做首尾帧、做封面）。
- **交互细节**：输入视频 → 位置选择（首帧/尾帧/指定秒数，可拖滑块预览）→ 输出图片节点。抽帧用 ffmpeg，秒级完成。
- **验收标准**：抽首帧、尾帧、指定秒各一次，输出的图片与视频对应位置一致。

### 模块 E：本地放大与补帧

#### E1 图片放大

- **用户故事**：作为用户，我想把一张图放大 4 倍变成 4K，用来当壁纸或打印。
- **交互细节**：
  - 输入：图片节点。输出：新的图片节点（原图保留）。
  - 参数：引擎（Real-ESRGAN / Real-CUGAN）、倍数（2/3/4）、模型（根据内容类型给出建议：`realesrgan-x4plus` 适合真人实拍、`realesrgan-x4plus-anime` 适合动画、`realesr-animevideov3` 小模型快）、tile 大小（默认 128，显存不足时界面会提示调小）。
  - 界面给出**明确的速度预期提示**（如"这张 1024x1024 的图，预计 10~30 秒"）。
  - 显示进度（ncnn 会输出处理进度）。
  - 处理中的显存/内存占用要显示出来（可选，但有助于用户理解为什么机器变慢）。
- **异常与边界**：
  - 输出尺寸过大（如放大后超过 16000px）→ 提示"放大后会超过 16000 像素，可能导致失败，是否继续？"
  - 显存不足（ncnn 报错）→ 自动把 tile 减半重试一次，仍失败则提示用户手动调小 tile。
  - 输入不是图片 → 拒绝并提示。
- **验收标准**：一张 1024x1024 的图放大 4 倍，输出 4096x4096 且画质明显提升；打开输出的文件确认尺寸正确。

#### E2 视频放大

- **用户故事**：作为用户，我想把一个 AI 生成的 720p 视频放大到 1080p 或 4K。
- **交互细节**：
  - 输入：视频节点。输出：新的视频节点。
  - 参数：倍数、模型、tile、是否保留音频、输出编码质量（CRF）。
  - **处理流程（必须严格按此实现，中间文件纪律是关键）**：
    1. 用 ffprobe 读取时长、帧率、总帧数。
    2. **估算所需磁盘空间**：总帧数 × 单帧 JPEG 大小（约 300 KB）× 2（放大的输出）。**若超过工作区可用空间的 50%，拒绝执行并告诉用户需要多少空间。**
    3. 分块抽帧：每批 200 帧，`ffmpeg -vsync 0 -f image2 -q:v 2 temp/frames/%08d.jpg`（**用 JPEG 不用 PNG**）。
    4. 对每批调用 `realesrgan-ncnn-vulkan.exe -i temp/frames -o temp/frames_up -n <model> -s <scale> -t <tile>`。
    5. 每批处理完立即合回视频段，并**删除该批的中间帧**。
    6. 最后把所有段拼接（`concat` demuxer），并**从原视频复制音轨**（`-c:a copy`，不重编码音频）。
    7. 清理整个 `temp/` 目录。
  - **进度显示**：必须显示"第 X/Y 帧"，而不是笼统的百分比。同时显示已用时间和**预估剩余时间**。
  - **可暂停/取消**：取消时立刻终止子进程并清理中间文件。
  - 界面明确提示这是后台任务，不能实时预览。
- **异常与边界**：
  - 视频没有音轨 → 跳过音频映射，不要报错（用 `-map 1:a?` 这种带问号的写法）。
  - 视频时长很长（>10 分钟）→ 提示可能耗时很久，建议先试剪短片段。
  - 中途失败 → 保留已完成的批次（可选），或整体清理（默认整体清理更省事）。
  - **磁盘不足 → 见流程第 2 步，必须在开始前拦截。**
- **验收标准**：一个 1080p 10 秒视频放大到 4K，输出可正常播放、有声、时长一致；处理完 `temp/` 目录为空；全过程磁盘峰值占用不超过预估值的 1.2 倍。

#### E3 补帧

- **用户故事**：作为用户，我想把 30fps 的视频变成 60fps，或者把慢放的视频变顺滑。
- **交互细节**：
  - 参数：倍率（2x/4x）、模型（`rife-v4.6` 等）、目标帧率（自动算出）。
  - 流程同 E2，但用 `rife-ncnn-vulkan.exe -i frames -o frames_2x -m <model> -n <multiplier>`。
  - **与慢放配合**：当用户把片段减速到 0.5 倍以下时，导出/预览界面要主动提示"要不要先补帧再慢放？否则画面会卡"。
- **异常与边界**：源视频已是高帧率（>60fps）→ 提示没必要补帧。补帧后文件巨大 → 提示输出体积。
- **验收标准**：30fps 视频补到 60fps，输出的帧率经 ffprobe 确认是 60，画面流畅。

#### E4 本地任务队列

- **用户故事**：作为用户，我希望我把几个放大任务都点上，它们会排队一个个跑，而不是同时跑把机器卡死。
- **交互细节**：
  - 全局任务中心（右下角可折叠面板），列出所有排队/运行/已完成/失败的任务。
  - **本地任务严格串行**（同时 1 个）。界面上显示"排队中（前面还有 2 个）"。
  - 每个任务显示：名称、状态、进度、已用时间、预估剩余。
  - 支持取消单个任务、取消全部、失败重试。
  - 任务完成后弹出通知（页面内 toast + 标题栏闪烁提示）。
- **验收标准**：连续点 3 个视频放大任务，确认它们是依次执行而非并行；取消排队中的任务有效。

### 模块 F：时间轴与视频剪辑

#### F1 时间轴基础

- **用户故事**：作为用户，我想把画布上的视频拖到时间轴上，排成一条片子。
- **交互细节**：
  - 底部时间轴面板，可折叠、可调整高度。
  - **把画布上的视频/图片节点拖入时间轴**即可创建片段。也支持从素材库拖入。
  - 时间轴上层是标尺（显示时间刻度，格式 `mm:ss.f`），下层是轨道区。
  - 轨道：默认 1 条视频轨 + 1 条贴图轨。可新增轨道。
  - 播放头：竖向红线，可拖拽。拖动时上方预览画面跟随更新（基于 proxy，seek 精度到关键帧即可，标注"预览为关键帧精度，导出为帧精确"）。
  - 走带控制：播放/暂停、上一帧/下一帧、跳到开头/结尾、循环播放。显示当前时间码与总时长。
  - 缩放时间轴：`Ctrl+滚轮` 缩放时间轴比例；滚轮水平滚动。
  - 片段显示：缩略图条 + 文件名 + 时长；变速的片段显示倍速角标（如 `2.0x`）。
- **实现要求**：
  - 时间轴用 Konva 或纯 Canvas 绘制（这里用 Konva 是合适的，因为是大量图形元素且没有富交互控件）。也可以用 DOM，但片段多时 DOM 性能更差。**建议用 Konva，这是本 PRD 中 Konva 的唯一推荐用途。**
  - 时间轴状态存在独立的 store，**不要和画布状态混在一个 store 里**。
- **验收标准**：拖 3 个视频进时间轴形成连续序列；播放能顺次播放完；拖动播放头预览画面跟随。

#### F2 分割与删除

- **用户故事**：作为用户，我想把一段视频中间不要的部分切掉。
- **交互细节**：
  - 播放头停在某位置，按 `S` 或点"分割"按钮，把播放头所在的片段一分为二（**如果是视频轨，两条轨道上同一时间位置的片段一起切**）。
  - 选中片段按 `Delete` 删除，后面的片段**自动前移补齐空隙**（默认行为）；按住 Alt 删除则保留空隙。
  - 片段边缘可拖拽调整入点/出点（**这是裁剪，不是变速**）。
  - 吸附：拖拽片段时吸附到相邻片段边缘、播放头位置、时间轴起点。按住 Alt 临时关闭吸附。
- **异常与边界**：分割位置正好在片段边界 → 不执行并提示。删除后时间轴为空 → 显示"把素材拖到这里开始剪辑"。
- **验收标准**：分割、删除、前后移动都正确；删除后总时长正确减少。

#### F3 变速（时间重映射）—— 本项目最核心的剪辑功能

- **用户故事**：作为用户，我想在时间轴上抓住片段左右边缘拖动，把一段 10 秒的视频逐渐缩到 1 秒（变成 10 倍速），反过来也能把 1 秒拉长到 10 秒（变成慢放），而且声音跟着一起变。

- **交互细节（这是需求原文明确要求的功能，必须精确实现）**：
  - **拖拽片段右边缘**：向右拖长 → 该片段在时间轴上占用时间变长 → **速度变慢**；向左拖短 → 占用时间变短 → **速度变快**。
  - **拖拽片段左边缘**：改变片段在时间轴上的起点与占用长度，效果同样是改变速度（保持素材的入点不变，改变 outPoint 与 speed 的组合）。
  - **拖拽过程中实时显示当前倍速**（浮在片段上方，如 `0.5x` / `2.0x` / `10.0x`）。
  - **速度范围限制**：`0.1x` ~ `20.0x`。超出范围时边缘停住并显示"已达最慢/最快"。
  - **关键：拖拽边缘改变的是速度，不是裁剪。** 素材的 `outPoint - inPoint`（源时长）保持不变，改变的是 `speed`，从而改变 `timelineDurationSec = sourceDuration / speed`。
    - 10 秒素材拖成 1 秒：`speed = 10`，时间轴时长 1 秒，内容全保留（快速播放完整 10 秒内容）。
    - 1 秒素材拉成 10 秒：`speed = 0.1`，时间轴时长 10 秒，内容全保留（极慢播放）。
  - **两种模式可切换**（因为有些用户拖边缘是想裁剪）：片段左上角有一个小图标切换"变速模式"与"裁剪模式"。**默认是变速模式**（因为需求原文描述的是变速）。切换后拖拽边缘的行为不同，且图标和光标都要变化，避免误操作。
  - **变速与音频的处理**（必须正确）：
    - **视频**：使用 ffmpeg 的 `setpts`，系数 = `1/speed`。例如 10 倍速用 `setpts=0.1*PTS`；0.1 倍慢放用 `setpts=10*PTS`。
    - **音频**：使用 `atempo`，值 = `speed`。**`atempo` 在需要高质量时不能超过 2.0，必须串联。**
      - 10 倍速：`atempo=2.0,atempo=2.0,atempo=2.5`（2×2×2.5 = 10）
      - 0.1 倍慢放：`atempo=0.5,atempo=0.5,atempo=0.4`（0.5×0.5×0.4 = 0.1）
      - 实现要求：写一个 `buildAtempoChain(speed: number): string` 函数，自动把任意 speed 拆解成若干个 0.5~2.0 之间的因子串联。**这是必须单测的纯函数。**
    - **极慢放（speed < 0.5）时的画面卡顿问题**：0.1 倍慢放时每帧会重复显示约 10 次，画面像幻灯片。处理方式：
      1. 界面主动提示"当前为极慢放，画面可能不流畅。建议先补帧（变成 4 倍帧率）再慢放。"
      2. 提供一键操作："补帧后慢放"，自动生成"先补帧再慢放"的任务链。
      3. 兜底方案：若无 GPU 或用户拒绝补帧，使用 ffmpeg 内置的 `minterpolate` 做光流插值。**注意顺序：`minterpolate` 必须在 `setpts` 之前**（先插帧到高帧率，再拉长时长），否则会插出大量重复帧。
      4. `minterpolate` 极慢（1080p 约 1~5 fps）且对快速运动会有果冻伪影，**默认不启用，只在用户明确选择"用 CPU 补帧"时使用**。优先推荐 RIFE。
  - **变速时的音频模式**（片段级设置）：
    - `保留原声并变速`（默认）：atempo 串联，音调会变（10 倍速下人声本来就不可懂，这是正常的）
    - `静音`：`-an`，完全不要音频
    - `保留原声并保持音调`：若 ffmpeg 构建包含 `librubberband` 则用 `rubberband=tempo=<speed>`；**若不可用则降级为 atempo 并提示用户"当前 ffmpeg 不支持保持音调，已改用变速模式"**
    - 实现时可先用 `ffmpeg -filters` 检测 `rubberband` 是否存在，缓存检测结果。
- **异常与边界**：
  - speed 恰好为 1.0 → 不加任何变速滤镜（省性能）。
  - 拖拽使片段与相邻片段重叠 → 推到不重叠为止（或按 Alt 允许覆盖）。
  - 拖拽时超出速度范围 → 停在边界。
  - 片段没有音轨但用户选了保留原声 → 静默处理，不报错。
- **验收标准**：
  - 一段 10 秒视频拖成 1 秒，导出的 mp4 时长确实是 1 秒，内容是原来的完整 10 秒（快速播放）。
  - 一段 1 秒视频拉成 10 秒，导出的 mp4 时长确实是 10 秒。
  - 音频在 2 倍速、0.5 倍慢放时与画面对齐（用耳朵判断不跑偏）。
  - `buildAtempoChain` 对 0.1~20 的所有取值都能生成合法且乘积正确的链（单元测试覆盖边界值 0.1、0.25、0.5、1、2、4、10、20）。
- **产品所有者备注**：这一条是本项目最需要精细打磨的功能。开发时请把上面每个子项都当作硬需求实现，尤其是"拖边缘 = 变速而非裁剪"这个语义，以及变速必须同时正确处理视频和音频两条链。

#### F4 贴图叠加

- **用户故事**：作为用户，我想在视频上叠加一个贴图（logo、字幕条、装饰），固定在某个位置，只在某段时间出现。
- **交互细节**：
  - 在贴图轨上拖入图片素材创建贴图层。
  - **在预览画面上直接拖动贴图**调整位置、拖角调整大小，位置以归一化坐标（0~1）存储，这样换分辨率也不跑位。
  - 参数面板：透明度（0~1）、生效时间范围（相对片段起点的秒数）、淡入淡出时长。
  - 贴图可以有自己的轨道，也可以绑定到某个视频片段上（`Clip.overlays`）。**本期推荐绑定到片段**（更直观：跟着片段走）。
  - 多个贴图可叠加，按 z 顺序渲染。
- **异常与边界**：贴图超出画面 → 允许（部分出画是常见需求）。贴图时间范围超出片段长度 → 自动截断到片段范围。
- **验收标准**：在一个视频上叠一个贴图，指定第 3~8 秒显示、位置在右下角，导出后贴图确实在对应时间和位置出现（用播放器逐秒检查）。

#### F5 调色

- **用户故事**：作为用户，我想调整视频的亮度、对比度、饱和度、色温，或者套用一个 LUT 风格。
- **交互细节**：
  - 片段级调色面板，控件为滑块：亮度、对比度、饱和度、伽马、色温、色调。
  - 提供预设（如"原片""明亮""电影感""暖调"），点击即填充滑块值。
  - 支持导入 `.cube` LUT 文件（存到工作区 `luts/`）。
  - **实时预览**：调色参数变化时，预览画面用 CSS filter 近似实时反馈（便宜、立刻），并明确标注"预览为近似效果，导出为精确效果"。
  - **一键应用到所有片段**：避免逐段调整。
- **实现映射（后端）**：

  | 界面参数 | ffmpeg 滤镜 |
  |---|---|
  | 亮度 / 对比度 / 饱和度 / 伽马 | `eq=brightness=B:contrast=C:saturation=S:gamma=G` |
  | 色温 / 色调 | `colorbalance=rs=..:rm=..:gs=..:rh=..:bs=..:bm=..:bh=..`（按色温色调换算出各通道偏移） |
  | LUT | `lut3d=file=<正斜杠路径>` |

  **注意：LUT 路径在 Windows 上必须用正斜杠**（`file=C:/Canvora/luts/x.cube`），反斜杠在 ffmpeg 滤镜语法里是转义字符，会导致静默失败。

- **异常与边界**：LUT 文件损坏 → 导出时报错，提示"LUT 文件无法读取，已跳过该 LUT"并继续导出（不因一个 LUT 让整个导出失败）。所有调色参数都是默认值 → 不加任何调色滤镜。
- **验收标准**：调到明显偏暖，导出后画面确实偏暖且与预览方向一致；套用 LUT 后导出画面有对应风格。

#### F6 播放头与预览

- **用户故事**：作为用户，我想拖动播放头看画面的变化，想按空格播放/暂停。
- **交互细节**：
  - 预览窗口按序列的宽高比显示，黑边填充。
  - 播放基于 proxy 文件，用多个 `<video>` 元素（每个轨道一个）同步播放，或按需切换 `src`。
  - **多段衔接处的处理**：播放跨片段边界时会有短暂加载，实现时预加载下一个片段的 proxy，减少卡顿。
  - 空格播放/暂停，`←/→` 前后一帧（基于 proxy 帧率，标注是近似），`Shift+←/→` 前后一秒，`Home/End` 到开头结尾。
  - 播放时显示"预览使用低清代理，导出为原画质"的常驻小字提示。
- **异常与边界**：proxy 还没生成好 → 显示"正在准备预览文件…"并提供进度，允许先看缩略图。proxy 生成失败 → 提示"预览文件生成失败"，但仍允许导出（导出用原片，不需要 proxy）。
- **验收标准**：3 段视频的序列能连续播放不中断超过 0.5 秒；拖动播放头画面跟随；proxy 未就绪时有明确提示。

### 模块 G：导出

#### G1 导出成片

- **用户故事**：作为用户，我想把时间轴上的内容导出成一条 mp4。
- **交互细节**：
  - 导出设置：文件名、输出目录（默认 `exports/`）、分辨率（默认取序列设置，可选 1080p/1440p/4K 或"跟随源"）、帧率（默认 30）、质量（CRF 滑块，默认 18，给出"体积/画质"的直观说明）、编码器（默认 `libx264`，若检测到 NVIDIA 显卡则提供 `h264_nvenc` 硬件编码选项并标注"更快但画质略低"）。
  - 进度：解析 ffmpeg 的结构化进度输出（`-progress pipe:1`），显示百分比、已用时间、预估剩余时间、当前帧率与码率。
  - 完成后：显示"导出完成"，提供"打开所在文件夹"和"在时间轴中回看"按钮，并把成品作为一个素材加入素材库。
  - 允许取消，取消后删除不完整的输出文件。
- **异常与边界**：
  - 磁盘空间不足 → 开始前估算（时长 × 码率）并检查，不足则拒绝。
  - 某个片段的素材文件丢失 → 导出前检查全部素材可用性，列出缺失项并拒绝导出（不要导出到一半才失败）。
  - ffmpeg 报错 → 把完整的 stderr 保存到 `exports/<name>.ffmpeg.log`，界面显示中文摘要 + "查看详细日志"链接。
  - 分辨率不一致的片段 → 统一缩放到导出分辨率（`scale` + `pad` 保持比例不拉伸，加黑边）。
  - 帧率不一致 → 统一到导出帧率。
- **验收标准**：
  - 3 段不同分辨率、不同帧率的视频拼成一条，导出的 mp4 在系统播放器正常播放，音画同步，时长与时间轴一致（误差 < 0.1 秒）。
  - 含变速、贴图、调色的序列导出后，效果与预览方向一致。
  - 导出失败时能给出中文原因且保留日志。

#### G2 导出首帧 / 尾帧

- **用户故事**：作为用户，我想快速把某个视频或整个序列的第一帧/最后一帧导出成图片。
- **交互细节**：
  - 两种入口：视频节点右键菜单；时间轴导出面板的"导出首帧/尾帧"。
  - 输出格式：PNG（无损）或 JPG（可选质量）。
  - 导出后：作为素材加入素材库，并在画布上创建一个图片节点。
- **实现命令**：
  - 首帧：`ffmpeg -i in.mp4 -frames:v 1 -q:v 2 first.jpg`
  - 尾帧：`ffmpeg -sseof -0.1 -i in.mp4 -frames:v 1 -q:v 2 last.jpg`
  - **注意**：`-sseof` 受关键帧对齐影响，`-0.1` 拿到的可能不是绝对最后一帧。要拿到绝对最后一帧，先用 ffprobe 取时长，再用精确 seek（`-ss <duration - 0.04>` 放在 `-i` 之后）取帧。**实现时用后者更可靠，并在验收时验证。**
- **验收标准**：对一个已知内容和时长的视频导出的首帧、尾帧，用图像查看器打开确认是画面的第一帧和最后一帧。

#### G3 导出预设

- **用户故事**：作为用户，我常用几种导出配置，不想每次重设。
- **交互细节**：可保存/删除/命名导出预设。内置 3 个预设："抖音竖屏 1080x1920"、"B站横屏 1920x1080"、"高画质 4K"。
- **验收标准**：保存一个预设后重开应用仍在，选择后参数被正确填充。

### 模块 H：设置、密钥与任务中心

#### H1 密钥管理

- **用户故事**：作为用户，我想集中管理所有 API 密钥，看得见我用在哪个服务商上。
- **交互细节**：
  - 列表显示：服务商、密钥尾 4 位、添加时间、测试状态。
  - 密钥输入框为密码类型，可切换显示。
  - **界面上常驻的醒目提示**："密钥保存在你本机。请只填写设置了消费上限的子密钥，不要使用主账号密钥。"
  - 支持删除、替换密钥。
  - 支持"一键测试全部"检查哪些密钥已失效。
- **异常与边界**：密钥文件损坏 → 提示"密钥文件损坏，需要重新填写"，不要让应用无法启动。
- **验收标准**：密钥不出现明文在界面默认状态、不出现在任何日志文件、不出现在 git 中。

#### H2 任务中心

- **用户故事**：作为用户，我想在一个地方看到我所有的任务。
- **交互细节**：列出全部任务，可按状态/类型/时间筛选；显示耗时与结果；失败任务可重试；已完成任务可定位到对应的画布节点或素材。
- **验收标准**：任务在应用重启后仍可查看；正在跑的云端任务在重启后恢复轮询。

#### H3 快捷键与帮助

- **用户故事**：作为用户，我记不住快捷键，想有个地方查。
- **交互细节**：`?` 或 `F1` 打开快捷键速查面板，按上下文分组（画布/时间轴/播放）。
- **验收标准**：面板中的所有快捷键与实际行为一致（**实现每个快捷键后回来核对这张表**）。

#### H4 应用日志

- **用户故事**：作为用户，当出问题时我希望有个日志能给我（或给开发 AI）看。
- **交互细节**：
  - 后端把日志写到工作区 `logs/canvora-YYYY-MM-DD.log`，按天切分，保留最近 7 天。
  - 界面设置页有"查看日志"和"导出诊断包"按钮（打包日志 + 版本 + 环境信息 + 设置，**不含密钥**）。
  - **日志里绝对不能出现密钥**。所有密钥在写日志前必须经过脱敏函数。
- **验收标准**：搜索日志文件搜不到任何密钥片段。

---

## 10. 关键算法与实现要点

### 10.1 滤镜图编译器（本项目最核心的后端模块）

**输入**：EDL（`Timeline` + 素材绝对路径解析结果）。
**输出**：一段 `filter_complex` 文本，写入临时文件。

**编译步骤：**

1. **校验**
   - 片段是否首尾相接（若有空隙，按背景色填充）。
   - `inPoint < outPoint`，`speed` 在 0.1~20。
   - 贴图时间窗在片段范围内。
   - 所有素材文件存在。

2. **为每个片段生成视频链**（每个片段作为独立的输入 `-i`）

   ```
   [i:v] trim=start=<inPoint>:end=<outPoint>, setpts=PTS-STARTPTS,
         [scale=..., pad=... 统一到输出尺寸],           ← 只在分辨率不一致时加
         [setpts=<1/speed>*PTS],                        ← 变速，speed≠1 时加
         [eq=... 调色], [colorbalance=...], [lut3d=...], ← 按需加
         setsar=1
         [v<i>]
   ```

   **顺序很重要**：`trim` → `setpts=PTS-STARTPTS`（把时间归零）→ `scale/pad` → `setpts`（变速）→ 调色。

3. **为每个片段生成音频链**

   ```
   [i:a] atrim=start=<inPoint>:end=<outPoint>, asetpts=PTS-STARTPTS,
         <atempo 串联链>,                                ← speed≠1 且需保原声时加
         aresample=48000
         [a<i>]
   ```
   - 无音轨的片段：跳过，并在拼接时用 `anullsrc` 补静音（否则 concat 会失败）。**这是常见坑，必须处理。**
   - `audioMode == 'mute'`：跳过，补静音。

4. **拼接**

   ```
   [v0][a0][v1][a1][v2][a2] concat=n=3:v=1:a=1 [vcat][acat]
   ```

5. **贴图叠加**（在拼接之后、按时间轴绝对时间计算 enable 窗口）

   ```
   [<logoIdx>:v] scale=<w>:<h>, format=rgba [logo]
   [vcat][logo] overlay=x=<X>:y=<Y>:enable='between(t\,<start>\,<end>)' [vov]
   ```
   - 位置用归一化坐标乘以输出宽高换算。
   - 淡入淡出：在贴图上加 `format=rgba,fade=in:st=<s>:d=<d>:alpha=1,fade=out:...`。
   - 多个贴图串联处理。

6. **输出映射**

   ```
   -map "[vov]" -map "[acat]"
   -c:v libx264 -crf 18 -preset medium -pix_fmt yuv420p
   -c:a aac -b:a 192k
   -movflags +faststart
   ```

**实现要求：**
- **必须写单元测试**：给定一个构造好的 EDL，断言生成的滤镜图字符串包含预期的片段数量、正确的 `setpts` 系数、正确的 `atempo` 链、正确的 `between` 时间窗。这是整个项目最需要测试覆盖的地方，因为它编译出来的东西一旦错了，用户只能通过"导出的视频不对"来发现。
- **`\.` 转义**：`between(t\,3\,8)` 里的逗号在 filter_complex 里必须转义。**走 `-filter_complex_script` 文件时仍需转义内部逗号**（文件方式解决的是命令行引号问题，不解决滤镜语法里的逗号分隔问题）。这一点容易搞错，测试必须覆盖。
- 滤镜图文本可能很长（多片段时超过 8KB），**这正是必须用 `-filter_complex_script` 的原因**（命令行有长度上限）。

### 10.2 `buildAtempoChain` 纯函数

```
输入：speed（0.1 ~ 20）
输出：如 "atempo=2.0,atempo=2.0,atempo=2.5" 的字符串

规则：
- speed == 1 → 返回空字符串
- 循环：当剩余因子 > 2.0 时，取 2.0；当剩余因子 < 0.5 时，取 0.5
- 直到剩余因子落在 [0.5, 2.0] 区间，作为最后一个因子
- 因子保留 2 位小数，乘积必须等于 speed（允许 1e-6 误差）
```

**必须单测的输入**：0.1、0.2、0.25、0.5、0.75、1、1.5、2、3、4、5、10、15、20。

### 10.3 素材归一化（导入时执行）

**目的**：消除预览与导出的三类不一致来源（见 6.6）。

**规则**：满足任一条件就需要归一化：
- ffprobe 报告存在 `rotate` 显示矩阵且不为 0。
- `pix_fmt` 不是 `yuv420p`。
- 色彩空间不是 BT.709。
- 视频编码不是浏览器普遍支持的 H.264（如 HEVC、AV1 在部分环境不稳）。

**归一化命令：**
```
ffmpeg -i in.mp4 -vf "scale='min(1920,iw)':-2" \
  -c:v libx264 -preset veryfast -crf 20 -pix_fmt yuv420p -colorspace bt709 \
  -c:a aac -b:a 192k -movflags +faststart out.mp4
```

**注意**：归一化只用于**编辑与预览的基准**。原始文件始终保留在 `assets/`，导出时若用户选择"最高画质"可直接用原片（但要接受旋转/色彩偏差风险）。**默认导出使用归一化版本**，这样预览与导出必然一致。

### 10.4 proxy 生成与缓存

- 触发时机：素材导入后、生成/放大完成后，自动排队。
- 命令：`ffmpeg -i in.mp4 -vf "scale=-2:480" -c:v libx264 -crf 30 -preset veryfast -g 30 -movflags +faststart -c:a aac -b:a 64k out.proxy.mp4`
- `-g 30` 是关键：关键帧间隔 1 秒，保证拖动播放头时 seek 快。
- 缓存策略：以 `assetId` 命名；素材归一化版本变化时失效重建。删除素材时一并删除 proxy。
- `proxyStatus` 在数据库里跟踪，前端据此决定显示"准备预览中"还是播放器。

### 10.5 节点执行引擎

```
executeNode(nodeId):
  order = topologicalSort(nodeId)        // DFS 后序，含环检测
  for n in order:
    if not isDirty(n): continue          // 参数未变且已成功 → 复用结果
    inputs = collectInputs(n)            // 按端口 kind 从上游结果中提取
    validateInputs(n, inputs)            // 缺输入则失败并明确提示
    result = runNode(n, inputs)          // 提交云端任务 或 本地队列
    saveResult(n, result)
  return lastResult
```

**isDirty 判定**：节点记录 `lastRunHash`（参数 + 上游结果 ID 的哈希）。哈希一致且上次成功 → 不算脏。

**环检测**：DFS 时用三种颜色标记（白=未访问、灰=在栈中、黑=已完成）。遇到灰节点即发现环，报出环上的节点 id 列表，前端高亮这些节点。

**并发限流**：
- 本地任务：串行队列，同时 1 个。
- 云端任务：`p-limit` 之类的并发控制，默认 3。
- 混合：本地任务启动前必须等待云端任务全部结束（或反过来），避免内存争抢。

### 10.6 4K 抽帧的磁盘保护

```
framesTotal = durationSec * fps
estimatedBytes = framesTotal * 350KB * 2      // 输入 JPEG + 输出 JPEG
if estimatedBytes > freeSpace * 0.5:
    reject("需要约 X GB 空间，当前工作区只剩 Y GB")
batchSize = 200
for each batch:
    extract → upscale → encode segment → delete batch frames
```

**要求**：处理期间界面显示"当前磁盘占用"与"已清理批次数"，让用户有信心它在清理。

---

## 11. 分期路线图

每期结束都必须是一个**能真实使用的状态**，而不是半成品。

### P0：能日常用起来（先跑通"画布 + AI 生成"）

**可交付物**：打开 Canvora，能导入素材、在无限画布上摆放、连成流程、调用云端 AI 生成图片和视频、结果落到画布上。

| 模块 | 内容 |
|---|---|
| A | A1 工作区初始化、A2 素材导入、A3 素材库、A4 多项目管理 |
| B | B1 视口、B2 节点、B3 连线、B4 执行引擎、B5 导入导出 |
| C | C1 服务商管理、C2 能力映射 |
| D | D1 文本节点、D2 生图节点、D3 生视频节点（含首尾帧）、D4 抽帧节点 |
| H | H1 密钥、H2 任务中心、H3 快捷键、H4 日志 |

**完成判据**：本 PRD 第 2.4 节的"场景一"和"场景二"能完整走通，无阻塞性 bug。

**这一期不包含**：任何放大/补帧能力、任何剪辑能力。

### P1：差异化核心（本地 AI + 剪辑）

**可交付物**：能把素材放大到 4K、补帧，能把多段素材剪成一条片子并导出。

| 模块 | 内容 |
|---|---|
| E | E1 图片放大、E2 视频放大、E3 补帧、E4 本地任务队列 |
| F | F1 时间轴基础、F2 分割删除、F3 **变速**、F4 贴图、F5 调色、F6 播放头预览 |
| G | G1 导出成片、G2 导出首尾帧、G3 导出预设 |

**完成判据**：本 PRD 第 2.4 节的"场景三"能完整走通；F3 的所有验收标准全部通过。

### P2：打磨与增强

- 任务队列持久化与断点续跑完善
- 批量处理（比如一次选中 10 张图全部放大）
- 性能优化（画布在 200 节点以上的表现、proxy 预生成策略）
- 快捷键自定义
- 转场特效（叠化等）
- **可选**：Tauri 外壳（把本地网页版变成双击即开的 Windows 应用）
- **可选**：换用 BtbN 构建的 ffmpeg 以启用 libplacebo，从而支持 Anime4K shader

---

## 12. 验收测试清单

每一项都应该能明确判定"通过 / 不通过"。交付时说清哪些通过、哪些没测。

### 12.1 环境与启动

- [ ] 全新环境（删掉工作区配置）启动，引导页正确列出各磁盘剩余空间，且默认选中剩余最大的盘
- [ ] C 盘剩余低于 30 GB 时在引导页标注不推荐
- [ ] 工作区目录结构按 6.7 节完整创建
- [ ] ffmpeg 缺失时给出明确指引且不阻塞进入应用
- [ ] 冷启动（双击脚本到界面可用）在 10 秒内
- [ ] 应用运行期间，工作区所在磁盘的 `temp/` 目录在任务结束后为空

### 12.2 画布

- [ ] 100 个节点的画布，缩放平移流畅无明显掉帧
- [ ] 指针锚点缩放准确（缩放前后指针下的节点位置不漂移）
- [ ] 拖动 20 个节点同时移动不卡顿
- [ ] 从输出口拖到非法输入口时，非法口显示为灰色且松手无效
- [ ] 构造一个环（A→B→A），连线被拒绝并提示
- [ ] 视频节点同一时刻只有一个在播放
- [ ] 视频节点右键抽首帧/尾帧，生成的图片与视频一致
- [ ] 画布导出为 `.canvora` 文件，重新导入后节点、连线、位置完全一致

### 12.3 执行引擎

- [ ] "提示词 → 生图 → 放大"三段链，点最后节点运行，三步自动依次完成
- [ ] 中间一步失败时，已成功的步骤结果保留，重试只重跑失败部分
- [ ] 执行中关闭浏览器页面，后端继续执行；重新打开页面后进度正确恢复
- [ ] 取消功能真正终止任务（云端任务确实停止，本地任务子进程确实被杀掉，任务管理器里看不到残留的 ffmpeg.exe）
- [ ] 连续发起 3 个云端生成任务，并发数不超过设置值
- [ ] 连续发起 3 个本地放大任务，确认是串行执行

### 12.4 AI 接口

- [ ] 用阿里云百炼的 Key 测试连接成功
- [ ] 添加一个自定义的 OpenAI 兼容服务商并能成功调用
- [ ] 生图节点上看不到纯文本模型（能力筛选生效）
- [ ] 没有任何生视频模型时，下拉框给出引导而非空白
- [ ] 首尾帧生视频：给两张图能成功生成视频
- [ ] 生成结果在 24 小时后仍然可用（因为已下载落盘，不依赖会过期的 URL）
- [ ] 内容审核被拦截时给出明确中文提示
- [ ] 余额不足时给出明确中文提示

### 12.5 本地放大与补帧

- [ ] 1024x1024 图片放大 4 倍，输出 4096x4096（用图片查看器确认尺寸）
- [ ] 1080p 10 秒视频放大 2 倍，输出正常播放、有声、时长一致
- [ ] 视频放大过程中，4K 素材的临时文件峰值占用不超过预估值的 1.2 倍
- [ ] 磁盘空间不足时在开始前就被拒绝并给出所需空间
- [ ] 30fps 视频补帧到 60fps，ffprobe 确认输出帧率为 60
- [ ] 视频无音轨时放大不报错（输出也无音轨即可）
- [ ] 取消放大任务后，任务管理器无残留的 realesrgan 进程

### 12.6 剪辑与导出

- [ ] 拖 3 个视频进时间轴，形成连续序列，能顺次播放完
- [ ] 在片段中间分割，前后两段时长之和等于原时长
- [ ] 删除中间片段后，后面片段自动前移，总时长正确减少
- [ ] **10 秒视频右边缘拖到 1 秒，导出 mp4 时长为 1 秒，内容是完整 10 秒内容**
- [ ] **1 秒视频右边缘拉到 10 秒，导出 mp4 时长为 10 秒**
- [ ] 变速时音频跟随（2 倍速和 0.5 倍慢放各测一次，耳朵判断不跑偏）
- [ ] `buildAtempoChain` 单测通过（覆盖 0.1、0.25、0.5、1、2、4、10、20）
- [ ] 叠一个贴图在第 3~8 秒、右下角，导出后逐秒检查位置和时间都对
- [ ] 调到明显偏暖，导出画面确实偏暖
- [ ] 导入一个 `.cube` LUT，导出后画面有对应风格
- [ ] 3 段不同分辨率、不同帧率的视频拼接导出，播放正常、音画同步、时长误差 < 0.1 秒
- [ ] 导出失败时保留 `.ffmpeg.log` 并给出中文原因
- [ ] 导出首帧/尾帧，与视频实际首尾帧一致
- [ ] 导出后 `temp/` 目录为空

### 12.7 安全与合规

- [ ] 密钥不以明文出现在界面默认状态、日志文件、任何提交到 git 的文件中
- [ ] `.gitignore` 包含 `secrets.json`
- [ ] `DEPENDENCIES.md` 中每个依赖的许可证都是 MIT / Apache-2.0 / BSD 系列
- [ ] 没有引入 6.4 节黑名单中的任何项目
- [ ] 导入的图片没有被自动上传到任何公共服务（除用户明确配置的接口外）

---

## 13. 风险登记册

### 13.1 高风险

| 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|
| **16 GB 内存被吃满导致系统卡死** | 高 | 严重 | 本地任务严格串行；视频处理全部分块流式；前端不缓存大文件；界面上显示内存占用。**这是第 5.2 节限制 1 存在的原因** |
| **4K 抽帧写满磁盘** | 中 | 严重 | 中间帧用 JPEG；分块处理完即删；开始前估算空间并拦截；工作区默认不在 C 盘 |
| **滤镜图编译错误导致导出结果不对** | 高 | 严重 | `buildAtempoChain` 与滤镜图编译都必须有单元测试；验收清单里变速和贴图的测试项必须实做 |
| **预览与导出一致性翻车（旋转/偏色）** | 高 | 中 | 导入时归一化转码；预览与导出共用同一份滤镜定义 |
| 云端 AI 接口改版导致适配器失效 | 高 | 中 | 适配器独立、可单独更新；模型 ID 写进配置而不是硬编码；接口错误信息原样保留供排查 |
| 首尾帧接口要求公网图片 URL | 中 | 中 | 优先用 base64；不支持则明确提示用户配置图床；**绝不私自上传用户图片到公共图床** |

### 13.2 中风险

| 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|
| 画布节点多时性能下降 | 中 | 中 | 视口裁剪、`React.memo` + 精确订阅、LOD、交互期 `will-change` |
| 视频放大耗时超预期，用户以为卡死 | 高 | 中 | 显示"第 X/Y 帧"、已用时间、预估剩余；明确说明是后台任务 |
| ffmpeg 命令的 Windows 路径转义问题 | 高 | 中 | 一律用 `-filter_complex_script`；LUT 路径用正斜杠；素材重命名为无中文无空格的 ID 文件名 |
| ffmpeg sidecar/子进程杀不干净，残留进程吃 CPU | 中 | 中 | 取消时杀整个进程树（Windows 用 `taskkill /F /T /PID`）；应用退出时清理；任务中心提供"清理残留进程" |
| 512 单位以上的 `atempo` 链计算错误 | 低 | 中 | 单元测试覆盖全部边界值 |
| 变慢放时画面卡顿（帧重复） | 高 | 中 | 主动提示 + 一键"补帧后慢放"；兜底 `minterpolate`（注意顺序） |

### 13.3 需要用户知晓的风险（写进 README）

| 风险 | 说明 |
|---|---|
| **API 密钥无法真正保密** | 本地存储只能提高门槛。请只填写有消费上限的子密钥 |
| **不做自动更新** | 出于安全考虑本版本不实现自动更新（参考：某同类开源项目有从 GitHub 拉代码覆盖自己的端点，这是桌面应用里必须避免的行为）。升级需手动替换文件 |
| **火山 Seedance 视频 API 个人拿不到** | 官方明确只面向企业认证主体。视频生成只能走阿里万相 / 智谱 CogVideoX / MiniMax |
| **可灵 API 需要白名单审核** | 且预付费起步约 700 美元 |
| **DeepSeek 不能生图生视频** | 只能当文本模型用 |
| 每次使用需手动启动 | 双击启动脚本；这是"开发最快"的代价 |
| 生成的视频画质受服务商限制 | 放大只能改善分辨率，不能凭空补出细节 |

---

## 14. 附录

### 14.1 可直接使用的 ffmpeg 命令集

以下命令均已在调研中核实语法，开发时可直接使用（**实际调用时必须用参数数组或 `-filter_complex_script`，不要拼 shell 字符串**）。

```bash
# ── 无损分割（秒级，不重编码）──────────────────────
ffmpeg -ss 10 -i in.mp4 -t 10 -c copy -avoid_negative_ts make_zero out.mp4

# ── 变速 10 倍（10s → 1s，保留音频）───────────────
ffmpeg -i in.mp4 -filter_complex \
  "[0:v]setpts=0.1*PTS[v];[0:a]atempo=2.0,atempo=2.0,atempo=2.5[a]" \
  -map "[v]" -map "[a]" -c:v libx264 -crf 18 -pix_fmt yuv420p out_10x.mp4

# ── 变速 10 倍（丢弃音频，最省事）─────────────────
ffmpeg -i in.mp4 -filter_complex "[0:v]setpts=0.1*PTS[v]" -map "[v]" -an \
  -c:v libx264 -crf 18 -pix_fmt yuv420p out_10x_noaudio.mp4

# ── 慢放 0.1 倍（1s → 10s，保留音频）──────────────
ffmpeg -i in.mp4 -filter_complex \
  "[0:v]setpts=10*PTS[v];[0:a]atempo=0.5,atempo=0.5,atempo=0.4[a]" \
  -map "[v]" -map "[a]" -c:v libx264 -crf 18 -pix_fmt yuv420p out_01x.mp4

# ── 极慢放 + CPU 光流补帧（注意顺序：先插帧再拉长）──
ffmpeg -i in.mp4 -filter_complex \
  "[0:v]minterpolate=fps=120:mi_mode=mci:mc_mode=aobmc:me_mode=bidir:vsbmc=1,setpts=10*PTS[v]" \
  -map "[v]" -an -c:v libx264 -crf 18 out_smooth_01x.mp4

# ── 首帧 / 尾帧 ──────────────────────────────────
ffmpeg -i in.mp4 -frames:v 1 -q:v 2 first.jpg
ffmpeg -sseof -0.1 -i in.mp4 -frames:v 1 -q:v 2 last.jpg
# 更可靠（绝对最后一帧）：先用 ffprobe 取时长 D，再用 -ss (D-0.04) 放在 -i 之后

# ── 贴图叠加（固定位置 + 时间窗 3~8 秒）───────────
ffmpeg -i base.mp4 -i logo.png -filter_complex \
  "[1:v]scale=200:-1[logo];[0:v][logo]overlay=x=W-w-20:y=20:enable='between(t\,3\,8)'[v]" \
  -map "[v]" -map 0:a? -c:a copy -c:v libx264 -crf 18 out.mp4

# ── 调色（eq → colorbalance → LUT 串联）──────────
ffmpeg -i in.mp4 -vf \
  "eq=contrast=1.08:saturation=1.15,colorbalance=rm=0.04:bm=0.06,lut3d=file=C:/Canvora/luts/kodak.cube" \
  -c:v libx264 -crf 18 out_graded.mp4

# ── 生成预览 proxy ───────────────────────────────
ffmpeg -i in.mp4 -vf "scale=-2:480" -c:v libx264 -crf 30 -preset veryfast -g 30 \
  -movflags +faststart -c:a aac -b:a 64k proxy.mp4

# ── 素材归一化（导入时）───────────────────────────
ffmpeg -i in.mp4 -vf "scale='min(1920,iw)':-2" \
  -c:v libx264 -preset veryfast -crf 20 -pix_fmt yuv420p -colorspace bt709 \
  -c:a aac -b:a 192k -movflags +faststart normalized.mp4

# ── 本地放大（Real-ESRGAN ncnn，BSD-3）───────────
ffmpeg -i in.mp4 -vsync 0 -f image2 -q:v 2 frames/%08d.jpg
realesrgan-ncnn-vulkan.exe -i frames -o frames_up -n realesrgan-x4plus -s 2 -t 128
ffmpeg -framerate 30 -i frames_up/%08d.jpg -i in.mp4 \
  -map 0:v -map 1:a? -c:v libx264 -crf 18 -pix_fmt yuv420p -c:a copy out_up.mp4

# ── 本地补帧（RIFE ncnn，MIT）────────────────────
rife-ncnn-vulkan.exe -i frames -o frames_2x -m rife-v4.6 -n 2
ffmpeg -framerate 60 -i frames_2x/%08d.png -i in.mp4 \
  -map 0:v -map 1:a? -c:v libx264 -crf 18 -pix_fmt yuv420p -c:a copy out_60fps.mp4
```

**模型选择的经验规则（写进界面的提示文案）：**

| 内容类型 | 图像放大模型 | 视频放大模型 |
|---|---|---|
| 动画 / 二次元 | `realesrgan-x4plus-anime` 或 Real-CUGAN | `realesr-animevideov3`（快） |
| 真人实拍 | `realesrgan-x4plus`，追求不发"塑料感"可用 Remacri | `realesr-generalv3`，2 倍为主 |
| 不确定 | `realesrgan-x4plus` | `realesr-animevideov3`（速度优先） |

**`realcugan` 和 Anime4K shader 都是动画训练的，用在真人实拍上会过度平滑失真，不要推荐给实拍内容。**

### 14.2 开源参考项目清单

| 项目 | 许可证 | 用途 |
|---|---|---|
| [basketikun/infinite-canvas](https://github.com/basketikun/infinite-canvas) | MIT | **主要参考**：画布实现、节点类型、连线模型、执行引擎思路、生成配置 UI |
| [omni-media/omniclip](https://github.com/omni-media/omniclip) | MIT | **推荐参考**：clip/effect 数据模型、时间轴交互、导出流程 |
| [schahriar/mfx](https://github.com/schahriar/mfx) | MIT | 编解码层参考 |
| [AmitDigga/fabric-video-editor](https://github.com/AmitDigga/fabric-video-editor) | MIT | 时间轴 UI 参考（注意它用 fabric.js） |
| [UnderHear/EaseCut](https://github.com/UnderHear/EaseCut)（npm `easecut-react`） | MIT | 备选时间轴组件，有中文文档 |
| [elahlabs/elah](https://github.com/elahlabs/elah) | Apache-2.0 | 备选时间轴组件 |
| [mifi/editly](https://github.com/mifi/editly) | MIT | 声明式视频合成 spec 设计参考 |
| [AcademySoftwareFoundation/OpenTimelineIO](https://github.com/AcademySoftwareFoundation/OpenTimelineIO) | Apache-2.0 | 时间轴数据模型参考（影视工业标准） |
| [xinntao/Real-ESRGAN](https://github.com/xinntao/Real-ESRGAN) | BSD-3-Clause | 本地放大二进制 |
| [nihui/rife-ncnn-vulkan](https://github.com/nihui/rife-ncnn-vulkan) | MIT | 本地补帧二进制 |
| [nihui/realcugan-ncnn-vulkan](https://github.com/nihui/realcugan-ncnn-vulkan) | MIT | 动画向放大（可选） |

**黑名单（禁止使用，理由见 6.4）：**
[tldraw](https://github.com/tldraw/tldraw)（生产需付费 license）、[Video2X](https://github.com/k4yt3x/video2x)（AGPL-3.0）、[ComfyUI](https://github.com/comfyanonymous/ComfyUI)（GPL-3.0 作依赖）、[twick](https://github.com/ncounterspecialist/twick)（SUL）、[openvideodev/react-video-editor](https://github.com/openvideodev/react-video-editor)（商业许可）、[hero8152/Infinite-Canvas](https://github.com/hero8152/Infinite-Canvas)（禁止商用）、Motz-Whiteboard（无许可证）。

### 14.3 AI 接口接入速查

| 服务商 | 能力 | 代表模型 | 鉴权 | 价格量级 | 异步方式 | 个人可开通 |
|---|---|---|---|---|---|---|
| 阿里云百炼 | 文生图/图生图/文生视频/图生视频/**首尾帧** | 图 `wanx2.1-t2i-plus`；视 `wan2.2-i2v-flash`；首尾帧 `wan2.2-kf2v-flash` | Bearer API Key | 图 0.14~0.54 元/张；视频 0.10~0.73 元/秒 | `services/aigc/*` 提交+轮询 | ✅ 个人实名 |
| 智谱 bigmodel | 文生图/文生视频/图生视频/**首尾帧** | `cogview-4`（0.06 元/张）、`cogvideox-3`（1 元/次） | Bearer API Key | 最低 | `videos.generations` + 查询 | ✅ 个人实名 |
| 火山方舟 | 文生图/图生图/**首尾帧视频（仅企业）** | `doubao-seedream-4-0-250828`、`doubao-seedance-2-0` | Bearer API Key | 图约 0.2 元/张；视频约 0.5~2 元/秒 | `/api/v3/contents/generations/tasks` | 图 ✅ / **视频 ❌ 仅企业** |
| MiniMax 海螺 | 文生/图生视频、原生音频 | `MiniMax-Hailuo-2.3`、`MiniMax-H3` | Bearer + GroupId | 768P 约 $0.08/秒 | `/v2/video_generation` + 轮询 | ✅ 个人实名 |
| 可灵 Kling | 文生/图生视频/**首尾帧** | `kling-v2.1`、`kling-v3.0` | **JWT HS256（AK/SK 本地签发，30 分钟）** | 试用包 $9.8/100 单位 | 提交+轮询 | ⚠️ 需白名单人工审核 |
| DeepSeek | **仅文本** | `deepseek-chat`、`deepseek-reasoner` | Bearer API Key | 约 1~2 元/百万 token | 同步 | ✅ 有 key 即可 |
| Google Gemini | 文生图/图生图/视频（Veo） | `gemini-3.1-flash-image`、`veo-3.1` | `x-goog-api-key` | 图 $0.034~0.134/张 | long-running operation | ⚠️ **需代理，大陆 IP 被拒** |
| OpenAI | 文生图；**视频 API 即将停用** | `gpt-image-2`；`sora-2`（2026-09-24 移除） | Bearer | 图约 $0.04~0.08/张 | `/v1/videos` | ⚠️ 需代理 + 海外卡 |
| fal.ai / Replicate | 聚合多家 | 含 Seedance、Kling、Wan、Flux | `Key $FAL_KEY` / Bearer | 按执行秒计 | 队列 + webhook | ⚠️ 需代理 + 美元卡 |

**重要提醒：模型 ID 常带日期后缀，服务商几个月就会换代。实现时模型 ID 必须从配置读取，不要硬编码在代码里。**

---

## 15. 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-09-11 | 首版。基于用户需求 + 四项联网调研（画布底座、本地 AI 模型、AI 接口、Tauri/剪辑架构）编写 |

---

**文档结束。** 开发前请先读 `AGENTS.md`（硬性约定）与 `TASKS.md`（任务拆解）。
